2026-01-10 08:03:10,283 INFO  [ckan.cli] Using configuration file /srv/app/src/ckanext-unaids/test.ini
2026-01-10 08:03:10,283 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 08:03:10,717 CRITI [ckanext.unaids.plugin] The unaids extension requires a database setup. Please run the following to create the database tables: 
ckan unaids initdb
2026-01-10 08:03:10,745 WARNI [ckan.common] Option lang is not declared
2026-01-10 08:03:10,767 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:10,769 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:10,808 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:10,956 WARNI [ckan.config.middleware.flask_app] Extensions are excluded from CSRF protection! We allow extensions to run without CSRF protection but it will be forced future releases. Read the documentation for more information on how to add CSRF protection to your extension.
============================= test session starts ==============================
platform linux -- Python 3.10.19, pytest-8.3.4, pluggy-1.6.0
rootdir: /srv/app/src/ckanext-unaids
plugins: mock-3.15.1, ckan-0.0.12, rerunfailures-15.0, Faker-26.0.0, split-0.10.0, cov-6.0.0, factoryboy-2.7.0, freezegun-0.4.2, retry-1.7.0, ckan-2.11.4
collected 162 items
2026-01-10 08:03:11,263 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates

ckanext/unaids/tests/test_actions.py EEEEEEEEEEEEEEEEEE                  [ 11%]
ckanext/unaids/tests/test_actions_dataset_lock.py ...FFF                 [ 14%]
ckanext/unaids/tests/test_actions_show_for_release.py EEEEEEEE           [ 19%]
ckanext/unaids/tests/test_auth.py EEEEEEEEEEEE                           [ 27%]
ckanext/unaids/tests/test_auth_logic.py .........EEEEEE.....EEEEEEE      [ 43%]
ckanext/unaids/tests/test_blueprints.py EEEEEEE                          [ 48%]
ckanext/unaids/tests/test_dataset_releases.py EEEEEEEEEEEEEEEEEE         [ 59%]
ckanext/unaids/tests/test_dataset_transfer.py EEEEEEEEEE                 [ 65%]
ckanext/unaids/tests/test_giftless_backend.py sE                         [ 66%]
ckanext/unaids/tests/test_helpers.py EEE                                 [ 68%]
ckanext/unaids/tests/test_logic.py EEEEEEEEEEEEEEEEEFFF                  [ 80%]
ckanext/unaids/tests/test_plugin.py EEEE..........                       [ 89%]
ckanext/unaids/tests/test_validators.py EEEEEEEEEEEEEEEEE                [100%]

==================================== ERRORS ====================================
____ ERROR at setup of TestGetTableSchema.test_schema_returned_successfully ____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:11,637 INFO  [ckan.model] Database tables dropped
2026-01-10 08:03:13,468 INFO  [ckan.model] CKAN database version upgraded: base -> 4a5e3465beb6 (head)
2026-01-10 08:03:13,468 INFO  [ckan.model] Database initialised
2026-01-10 08:03:13,468 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:13,699 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:13,784 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:13,785 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:13,837 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:244 Database tables dropped
INFO     ckan.model:__init__.py:376 CKAN database version upgraded: base -> 4a5e3465beb6 (head)
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestGetTableSchema.test_empty_dict_returned_for_no_schema __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:13,903 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:13,903 INFO  [ckan.model] Database initialised
2026-01-10 08:03:13,903 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:14,023 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:14,109 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:14,109 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:14,160 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestPackageActivityList.test_package_activity_list_containes_releases_names _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:14,216 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:14,217 INFO  [ckan.model] Database initialised
2026-01-10 08:03:14,217 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:14,293 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:14,380 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:14,381 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:14,394 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:14,434 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestFormatGuess.test_format_guess_csv[art.csv-text/csv-CSV] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:14,489 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:14,489 INFO  [ckan.model] Database initialised
2026-01-10 08:03:14,490 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:14,571 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:14,693 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestFormatGuess.test_format_guess_csv[anc.xls-application/vnd.ms-excel-XLS] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:14,758 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:14,758 INFO  [ckan.model] Database initialised
2026-01-10 08:03:14,758 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:14,872 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:15,005 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestFormatGuess.test_format_guess_csv[country_regions.geojson-application/geo+json-GeoJSON] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:15,063 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:15,063 INFO  [ckan.model] Database initialised
2026-01-10 08:03:15,063 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:15,147 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:15,272 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestFormatGuess.test_format_guess_csv[spectrum_file.pjnz-application/pjnz-PJNZ] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:15,342 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:15,342 INFO  [ckan.model] Database initialised
2026-01-10 08:03:15,342 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:15,411 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:15,539 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestFormatGuess.test_format_guess_csv[no_file_extension-None-None] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:15,647 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:15,648 INFO  [ckan.model] Database initialised
2026-01-10 08:03:15,648 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:15,720 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:15,860 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_____ ERROR at setup of TestFormatGuess.test_format_guess_csv[-None-None] ______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:15,919 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:15,920 INFO  [ckan.model] Database initialised
2026-01-10 08:03:15,920 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:15,992 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:16,117 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________________ ERROR at setup of TestUserShowMe.test_no_user _________________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:16,175 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:16,175 INFO  [ckan.model] Database initialised
2026-01-10 08:03:16,175 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:16,243 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:16,368 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
__________________ ERROR at setup of TestUserShowMe.test_user __________________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:16,467 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:16,467 INFO  [ckan.model] Database initialised
2026-01-10 08:03:16,467 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:16,539 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:16,662 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestPopulateDataDictionary.test_expected_use ________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:16,722 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:16,722 INFO  [ckan.model] Database initialised
2026-01-10 08:03:16,722 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:16,794 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:16,874 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:16,875 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:16,919 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____ ERROR at setup of TestUserAffiliation.test_user_show_with_affiliation _____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:16,979 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:16,979 INFO  [ckan.model] Database initialised
2026-01-10 08:03:16,979 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:17,098 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:17,232 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestUserAffiliation.test_user_list_all_fields_with_affiliation _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:17,291 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:17,291 INFO  [ckan.model] Database initialised
2026-01-10 08:03:17,291 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:17,362 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:17,501 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestUserAffiliation.test_user_list_usernames ________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:17,576 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:17,576 INFO  [ckan.model] Database initialised
2026-01-10 08:03:17,576 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:17,645 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:17,776 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestPackageCreate.test_should_complain_with_exception_when_dataset_type_invalid _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:17,836 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:17,836 INFO  [ckan.model] Database initialised
2026-01-10 08:03:17,837 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:17,950 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:18,032 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:18,033 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:18,077 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestPackageCreate.test_create_dataset_without_type_creates_one_with_default_type_of_dataset _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:18,137 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:18,138 INFO  [ckan.model] Database initialised
2026-01-10 08:03:18,138 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:18,218 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:18,303 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:18,304 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:18,346 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestPackageCreate.test_create_dataset_with_valid_type ____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:18,406 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:18,406 INFO  [ckan.model] Database initialised
2026-01-10 08:03:18,406 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:18,489 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:18,572 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:18,572 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:18,624 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_displays_release[id] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:27,013 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:27,013 INFO  [ckan.model] Database initialised
2026-01-10 08:03:27,013 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:27,093 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:27,238 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:27,239 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:27,260 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:27,300 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_displays_release[name] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:27,368 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:27,368 INFO  [ckan.model] Database initialised
2026-01-10 08:03:27,368 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:27,456 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:27,540 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:27,540 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:27,562 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:27,602 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_raises_when_incorrect_release _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:27,664 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:27,664 INFO  [ckan.model] Database initialised
2026-01-10 08:03:27,664 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:27,743 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:27,833 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:27,834 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:27,853 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:27,902 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_returns_correct_download_links _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:27,960 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:27,960 INFO  [ckan.model] Database initialised
2026-01-10 08:03:27,961 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:28,041 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:28,131 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:28,132 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:28,152 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:28,195 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_only_returns_updated_resource_download_links_for_uploads _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:28,304 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:28,304 INFO  [ckan.model] Database initialised
2026-01-10 08:03:28,304 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:28,385 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:28,467 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:28,468 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:28,485 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:28,526 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_auth[admin-True] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:28,599 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:28,600 INFO  [ckan.model] Database initialised
2026-01-10 08:03:28,600 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:28,684 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:28,770 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:28,771 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:28,789 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:28,828 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_auth[editor-True] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:28,900 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:28,900 INFO  [ckan.model] Database initialised
2026-01-10 08:03:28,900 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:28,985 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:29,075 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:29,075 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:29,093 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:29,138 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_auth[member-True] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:29,241 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:29,242 INFO  [ckan.model] Database initialised
2026-01-10 08:03:29,242 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:29,317 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:29,400 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:29,401 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:29,420 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:29,468 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_______ ERROR at setup of TestAuth.test_unaids_organization_update_valid _______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:29,543 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:29,543 INFO  [ckan.model] Database initialised
2026-01-10 08:03:29,544 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:29,621 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:29,709 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:29,713 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:29,713 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:29,753 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
______ ERROR at setup of TestAuth.test_unaids_organization_update_invalid ______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:29,813 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:29,813 INFO  [ckan.model] Database initialised
2026-01-10 08:03:29,813 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:29,894 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:29,986 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:29,989 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:29,990 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:30,029 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_________ ERROR at setup of TestAuth.test_dataset_lock[True-outcome0] __________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:30,139 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:30,139 INFO  [ckan.model] Database initialised
2026-01-10 08:03:30,139 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:30,222 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:30,311 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:30,315 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:30,315 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:30,354 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_________ ERROR at setup of TestAuth.test_dataset_lock[False-outcome1] _________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:30,416 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:30,416 INFO  [ckan.model] Database initialised
2026-01-10 08:03:30,416 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:30,485 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:30,578 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:30,582 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:30,582 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:30,623 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[False-False-False-outcome0] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:30,683 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:30,683 INFO  [ckan.model] Database initialised
2026-01-10 08:03:30,683 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:30,768 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:30,855 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:30,858 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:30,859 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:30,902 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[False-True-False-outcome1] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:31,009 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:31,009 INFO  [ckan.model] Database initialised
2026-01-10 08:03:31,009 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:31,089 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:31,181 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:31,185 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:31,186 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:31,225 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[False-False-True-outcome2] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:31,284 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:31,284 INFO  [ckan.model] Database initialised
2026-01-10 08:03:31,284 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:31,363 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:31,463 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:31,467 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:31,467 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:31,505 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[False-True-True-outcome3] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:31,571 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:31,571 INFO  [ckan.model] Database initialised
2026-01-10 08:03:31,571 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:31,651 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:31,737 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:31,740 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:31,741 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:31,786 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[True-False-False-outcome4] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:31,892 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:31,892 INFO  [ckan.model] Database initialised
2026-01-10 08:03:31,892 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:31,968 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:32,058 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:32,061 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:32,062 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:32,101 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[True-True-False-outcome5] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:32,160 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:32,160 INFO  [ckan.model] Database initialised
2026-01-10 08:03:32,160 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:32,239 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:32,330 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:32,334 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:32,335 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:32,373 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[True-False-True-outcome6] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:32,426 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:32,426 INFO  [ckan.model] Database initialised
2026-01-10 08:03:32,426 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:32,506 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:32,594 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:32,597 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:32,598 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:32,643 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[True-True-True-outcome7] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:32,750 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:32,750 INFO  [ckan.model] Database initialised
2026-01-10 08:03:32,750 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:32,833 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:32,923 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:32,927 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:32,928 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:32,972 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestVerifyRequiredScope.test_should_pass_when_scope_is_right _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:33,173 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:33,173 INFO  [ckan.model] Database initialised
2026-01-10 08:03:33,173 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:33,249 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:33,382 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestVerifyRequiredScope.test_should_complain_when_scope_is_none _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:33,447 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:33,447 INFO  [ckan.model] Database initialised
2026-01-10 08:03:33,447 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:33,525 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:33,701 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestVerifyRequiredScope.test_should_complain_when_scope_is_incorrect _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:33,761 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:33,761 INFO  [ckan.model] Database initialised
2026-01-10 08:03:33,761 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:33,840 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:33,958 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAccessTokenPresentAndValidAndUserAuthorized.test_should_set_user_when_token_correct_and_user_authorized _

make_app = <function _get_test_app at 0x77c87ec42f80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:34,018 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:34,018 INFO  [ckan.model] Database initialised
2026-01-10 08:03:34,018 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:34,023 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 08:03:34,097 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:34,245 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAccessTokenPresentAndValidAndUserAuthorized.test_should_return_false_when_no_token _

make_app = <function _get_test_app at 0x77c87ec42f80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:34,358 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:34,359 INFO  [ckan.model] Database initialised
2026-01-10 08:03:34,359 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:34,363 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 08:03:34,430 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:34,561 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAccessTokenPresentAndValidAndUserAuthorized.test_should_throw_when_subject_of_different_origin_than_auth0 _

make_app = <function _get_test_app at 0x77c87ec42f80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:34,636 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:34,636 INFO  [ckan.model] Database initialised
2026-01-10 08:03:34,636 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:34,641 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 08:03:34,719 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:34,856 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidateAndDecodeToken.test_should_validate_and_decode_token _

make_app = <function _get_test_app at 0x77c87ec42f80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:35,071 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:35,072 INFO  [ckan.model] Database initialised
2026-01-10 08:03:35,072 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:35,076 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 08:03:35,147 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:35,274 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidateAndDecodeToken.test_should_throw_when_no_matching_rsa_key _

make_app = <function _get_test_app at 0x77c87ec42f80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:35,341 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:35,341 INFO  [ckan.model] Database initialised
2026-01-10 08:03:35,341 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:35,346 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 08:03:35,415 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:35,540 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidateAndDecodeToken.test_should_throw_when_token_expired _

make_app = <function _get_test_app at 0x77c87ec42f80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:35,604 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:35,604 INFO  [ckan.model] Database initialised
2026-01-10 08:03:35,604 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:35,608 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 08:03:35,730 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:35,863 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidateAndDecodeToken.test_should_throw_when_incorrect_claims _

make_app = <function _get_test_app at 0x77c87ec42f80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:35,942 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:35,942 INFO  [ckan.model] Database initialised
2026-01-10 08:03:35,942 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:35,947 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 08:03:36,022 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:36,153 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidateAndDecodeToken.test_should_throw_when_general_exception_thrown _

make_app = <function _get_test_app at 0x77c87ec42f80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:36,214 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:36,215 INFO  [ckan.model] Database initialised
2026-01-10 08:03:36,215 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:36,219 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 08:03:36,296 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:36,415 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestRegressionOAuth2PluginDoesntPreventVanillaCkanAuthentication.test_using_api_key _

make_app = <function _get_test_app at 0x77c87ec42f80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:36,534 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:36,534 INFO  [ckan.model] Database initialised
2026-01-10 08:03:36,534 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:36,539 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 08:03:36,614 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:36,692 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:36,693 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:36,739 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestRegressionOAuth2PluginDoesntPreventVanillaCkanAuthentication.test_using_api_token _

make_app = <function _get_test_app at 0x77c87ec42f80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:36,810 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:36,810 INFO  [ckan.model] Database initialised
2026-01-10 08:03:36,810 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:36,815 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 08:03:36,887 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:36,967 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:36,968 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:37,012 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidateUserProfileBlueprint.test_annonymous_access_to_index_page _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:37,120 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:37,120 INFO  [ckan.model] Database initialised
2026-01-10 08:03:37,120 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:37,203 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:37,346 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
______ ERROR at setup of TestMemberLists.test_org_member_download_200_ok _______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:37,408 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:37,408 INFO  [ckan.model] Database initialised
2026-01-10 08:03:37,408 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:37,480 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:37,622 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestMemberLists.test_org_member_download_expected_columns __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:37,679 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:37,679 INFO  [ckan.model] Database initialised
2026-01-10 08:03:37,680 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:37,757 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:37,938 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
__ ERROR at setup of TestMemberLists.test_org_member_download_expected_emails __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:37,997 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:37,997 INFO  [ckan.model] Database initialised
2026-01-10 08:03:37,997 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:38,075 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:38,204 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestMemberLists.test_org_member_download_403 ________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:38,269 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:38,269 INFO  [ckan.model] Database initialised
2026-01-10 08:03:38,269 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:38,343 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:38,474 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestMemberLists.test_org_member_download_404 ________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:38,536 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:38,536 INFO  [ckan.model] Database initialised
2026-01-10 08:03:38,536 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:38,612 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:38,749 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestMemberLists.test_org_member_download_anonymous_access __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:38,854 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:38,854 INFO  [ckan.model] Database initialised
2026-01-10 08:03:38,854 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:38,930 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:39,056 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseCreateAndEdit.test_create_with_valid_user _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:39,116 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:39,116 INFO  [ckan.model] Database initialised
2026-01-10 08:03:39,116 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:39,191 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:39,277 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:39,277 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:39,297 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:39,345 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseCreateAndEdit.test_create_with_invalid_user _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:39,405 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:39,405 INFO  [ckan.model] Database initialised
2026-01-10 08:03:39,405 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:39,475 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:39,612 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:39,613 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:39,632 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:39,674 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseCreateAndEdit.test_create_with_existing_activity_id _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:39,736 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:39,736 INFO  [ckan.model] Database initialised
2026-01-10 08:03:39,737 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:39,811 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:39,893 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:39,893 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:39,912 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:39,954 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseCreateAndEdit.test_create_with_existing_release_name _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:40,009 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:40,010 INFO  [ckan.model] Database initialised
2026-01-10 08:03:40,010 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:40,088 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:40,175 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:40,175 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:40,193 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:40,237 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseCreateAndEdit.test_edit_with_valid_user __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:40,296 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:40,296 INFO  [ckan.model] Database initialised
2026-01-10 08:03:40,296 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:40,379 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:40,465 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:40,466 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:40,486 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:40,528 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseCreateAndEdit.test_edit_with_invalid_user _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:40,637 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:40,637 INFO  [ckan.model] Database initialised
2026-01-10 08:03:40,637 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:40,714 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:40,800 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:40,800 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:40,821 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:40,862 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____ ERROR at setup of TestDatasetReleaseDelete.test_delete_with_valid_user ____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:40,929 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:40,929 INFO  [ckan.model] Database initialised
2026-01-10 08:03:40,929 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:41,011 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:41,098 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:41,100 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:41,122 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:41,166 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestDatasetReleaseDelete.test_delete_with_invalid_user ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:41,223 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:41,223 INFO  [ckan.model] Database initialised
2026-01-10 08:03:41,223 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:41,308 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:41,392 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:41,393 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:41,412 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:41,453 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestDatasetReleaseRestore.test_restore_with_valid_user ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:41,568 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:41,568 INFO  [ckan.model] Database initialised
2026-01-10 08:03:41,568 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:41,649 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:41,740 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:41,741 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:41,761 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:41,811 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
__ ERROR at setup of TestDatasetReleaseRestore.test_restore_with_invalid_user __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:41,872 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:41,872 INFO  [ckan.model] Database initialised
2026-01-10 08:03:41,872 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:41,955 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:42,037 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:42,038 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:42,058 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:42,102 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____ ERROR at setup of TestDatasetReleaseListView.test_no_releases_created _____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:42,160 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:42,161 INFO  [ckan.model] Database initialised
2026-01-10 08:03:42,161 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:42,236 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:42,319 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:42,320 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:42,342 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:42,385 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseListView.test_releases_are_listed_to_owners _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:42,498 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:42,498 INFO  [ckan.model] Database initialised
2026-01-10 08:03:42,498 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:42,577 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:42,671 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:42,672 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:42,691 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:42,737 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseListView.test_releases_are_listed_to_outsiders _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:42,806 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:42,806 INFO  [ckan.model] Database initialised
2026-01-10 08:03:42,806 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:42,885 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:42,963 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:42,963 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:42,981 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:43,024 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseListView.test_add_release_button_is_shown_to_owners _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:43,086 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:43,086 INFO  [ckan.model] Database initialised
2026-01-10 08:03:43,086 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:43,166 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:43,248 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:43,249 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:43,271 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:43,312 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseListView.test_add_release_button_is_not_shown_to_outsiders _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:43,431 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:43,431 INFO  [ckan.model] Database initialised
2026-01-10 08:03:43,431 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:43,504 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:43,593 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:43,594 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:43,612 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:43,654 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestDatasetRead.test_activity_id_query_param ________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:43,714 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:43,714 INFO  [ckan.model] Database initialised
2026-01-10 08:03:43,715 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:43,791 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:43,871 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:43,872 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:43,894 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:43,936 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
______ ERROR at setup of TestDatasetRead.test_when_no_release_for_dataset ______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:43,993 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:43,993 INFO  [ckan.model] Database initialised
2026-01-10 08:03:43,993 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:44,073 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:44,163 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:44,164 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:44,183 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:44,227 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetRead.test_when_on_latest_version_of_dataset_has_release _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:44,334 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:44,334 INFO  [ckan.model] Database initialised
2026-01-10 08:03:44,334 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:44,411 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:44,496 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:44,497 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:44,520 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:44,564 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_collaborator_transfering_dataset __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:44,620 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:44,621 INFO  [ckan.model] Database initialised
2026-01-10 08:03:44,621 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:44,705 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:44,809 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:44,813 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:44,813 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:44,861 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_get_org_admins_with_email_addresses_two_admins _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:44,927 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:44,927 INFO  [ckan.model] Database initialised
2026-01-10 08:03:44,927 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:45,015 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:45,119 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:45,123 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:45,124 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:45,164 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_get_org_admins_with_email_addresses_one_admin _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:45,277 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:45,278 INFO  [ckan.model] Database initialised
2026-01-10 08:03:45,278 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:45,357 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:45,463 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:45,467 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:45,468 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:45,509 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_get_org_admins_with_email_addresses_no_admins _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:45,570 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:45,570 INFO  [ckan.model] Database initialised
2026-01-10 08:03:45,570 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:45,647 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:45,752 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:45,756 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:45,757 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:45,797 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_get_org_admins_with_email_addresses_two_orgs _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:45,866 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:45,866 INFO  [ckan.model] Database initialised
2026-01-10 08:03:45,866 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:45,948 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:46,054 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:46,058 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:46,059 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:46,098 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_get_org_admins_with_no_users_in_org _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:46,204 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:46,204 INFO  [ckan.model] Database initialised
2026-01-10 08:03:46,204 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:46,280 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:46,382 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:46,385 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:46,386 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:46,431 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestDatasetTransfer.test_send_dataset_transfer_emails ____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:46,493 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:46,493 INFO  [ckan.model] Database initialised
2026-01-10 08:03:46,493 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:46,566 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:46,674 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:46,678 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:46,678 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:46,721 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_send_dataset_transfer_email_subject _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:46,786 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:46,786 INFO  [ckan.model] Database initialised
2026-01-10 08:03:46,786 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:46,864 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:46,976 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:46,979 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:46,980 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:47,016 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_send_dataset_transfer_emails_errors _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:47,130 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:47,130 INFO  [ckan.model] Database initialised
2026-01-10 08:03:47,130 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:47,207 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:47,315 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:47,319 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:47,320 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:47,357 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_____ ERROR at setup of TestDatasetTransfer.test_dataset_transfer_request ______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:47,419 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:47,419 INFO  [ckan.model] Database initialised
2026-01-10 08:03:47,419 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:47,499 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:47,605 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:47,608 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:47,609 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:47,646 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
__ ERROR at setup of TestResourceUrlEncoding.test_resource_url_encoding_test ___

make_app = <function _get_test_app at 0x77c87ec42f80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:47,714 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:47,714 INFO  [ckan.model] Database initialised
2026-01-10 08:03:47,714 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:47,719 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 08:03:47,798 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:47,877 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:47,877 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:47,938 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestDatasetLockHelper.test_dataset_lockable _________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:48,060 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:48,060 INFO  [ckan.model] Database initialised
2026-01-10 08:03:48,060 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:48,142 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:48,231 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:48,235 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:48,235 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:48,277 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
______ ERROR at setup of TestDatasetLockHelper.test_dataset_not_lockable _______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:48,336 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:48,337 INFO  [ckan.model] Database initialised
2026-01-10 08:03:48,337 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:48,412 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:48,505 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:48,509 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:48,509 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:48,546 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestDatasetLockHelper.test_dataset_not_found ________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:48,610 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:48,610 INFO  [ckan.model] Database initialised
2026-01-10 08:03:48,611 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:48,691 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:48,782 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:48,786 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:48,787 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:48,828 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[validates if all upload fields correct] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:48,940 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:48,940 INFO  [ckan.model] Database initialised
2026-01-10 08:03:48,940 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:49,025 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:49,151 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[validates if no upload fields present] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:49,211 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:49,211 INFO  [ckan.model] Database initialised
2026-01-10 08:03:49,211 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:49,288 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:49,424 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[validates if all fields are present but empty] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:49,483 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:49,483 INFO  [ckan.model] Database initialised
2026-01-10 08:03:49,483 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:49,558 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:49,741 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if some fields missing and others set to empty string] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:49,802 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:49,802 INFO  [ckan.model] Database initialised
2026-01-10 08:03:49,802 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:49,885 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:50,007 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
__ ERROR at setup of test_validate_resource_upload_fields[fails if size None] __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:50,068 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:50,069 INFO  [ckan.model] Database initialised
2026-01-10 08:03:50,069 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:50,146 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:50,273 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if only size present] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:50,328 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:50,328 INFO  [ckan.model] Database initialised
2026-01-10 08:03:50,328 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:50,408 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:50,530 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if sha256 empty string] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:50,638 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:50,638 INFO  [ckan.model] Database initialised
2026-01-10 08:03:50,639 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:50,716 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:50,848 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if sha256 None] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:50,913 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:50,914 INFO  [ckan.model] Database initialised
2026-01-10 08:03:50,914 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:50,982 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:51,108 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if only sha256 present] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:51,169 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:51,169 INFO  [ckan.model] Database initialised
2026-01-10 08:03:51,169 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:51,251 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:51,386 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if invalid sha256] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:51,449 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:51,449 INFO  [ckan.model] Database initialised
2026-01-10 08:03:51,449 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:51,569 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:51,689 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if lfs_prefix empty string] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:51,745 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:51,745 INFO  [ckan.model] Database initialised
2026-01-10 08:03:51,745 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:51,829 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:51,967 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if lfs_prefix None] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:52,034 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:52,034 INFO  [ckan.model] Database initialised
2026-01-10 08:03:52,034 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:52,111 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:52,247 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if only lfs_prefix present] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:52,306 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:52,306 INFO  [ckan.model] Database initialised
2026-01-10 08:03:52,306 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:52,388 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:52,566 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of test_update_filename_in_upload_resource_url _________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:52,633 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:52,633 INFO  [ckan.model] Database initialised
2026-01-10 08:03:52,633 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:52,711 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:52,798 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:52,799 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:52,846 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____ ERROR at setup of test_update_filename_in_link_resource_url[http url] _____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:52,909 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:52,909 INFO  [ckan.model] Database initialised
2026-01-10 08:03:52,909 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:52,986 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:53,071 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:53,071 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:53,123 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____ ERROR at setup of test_update_filename_in_link_resource_url[https url] ____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:53,189 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:53,189 INFO  [ckan.model] Database initialised
2026-01-10 08:03:53,189 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:53,269 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:53,356 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:53,357 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:53,408 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
__ ERROR at setup of test_update_filename_in_link_resource_url[url with path] __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:53,529 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:53,530 INFO  [ckan.model] Database initialised
2026-01-10 08:03:53,530 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:53,617 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:53,705 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:53,706 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:53,753 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
______ ERROR at setup of TestPlugin.test_geojson_format_guessed_correctly ______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:54,176 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:54,176 INFO  [ckan.model] Database initialised
2026-01-10 08:03:54,176 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:54,263 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:54,350 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:54,350 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:54,398 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestPlugin.test_blob_storage_validator_is_used_during_resource_actions _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:54,453 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:54,453 INFO  [ckan.model] Database initialised
2026-01-10 08:03:54,453 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:54,529 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:54,614 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:54,615 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:54,665 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_________ ERROR at setup of TestValidatePackage.test_validate_package __________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:164: in load
    service = _get_service(plugin)
../ckan/ckan/plugins/core.py:284: in _get_service
    return plugin.load()(name=plugin_name)
/usr/local/lib/python3.10/site-packages/pkg_resources/__init__.py:2777: in load
    return self.resolve()
/usr/local/lib/python3.10/site-packages/pkg_resources/__init__.py:2783: in resolve
    module = __import__(self.module_name, fromlist=['__name__'], level=0)
../ckanext-validation/ckanext/validation/plugin/__init__.py:11: in <module>
    from ckanext.validation.logic import (
../ckanext-validation/ckanext/validation/logic.py:16: in <module>
    from ckanext.validation.jobs import run_validation_job
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

    # encoding: utf-8
    
    import logging
    import datetime
    import json
    import re
    
    import requests
    from sqlalchemy.orm.exc import NoResultFound
>   from frictionless import system, Resource, Package, Report, Schema, Dialect, Check, Checklist, Detector
E   ModuleNotFoundError: No module named 'frictionless'

../ckanext-validation/ckanext/validation/jobs.py:10: ModuleNotFoundError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:54,726 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:54,726 INFO  [ckan.model] Database initialised
2026-01-10 08:03:54,727 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:54,805 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidatePackage.test_metadata_change_does_not_validate_package _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:164: in load
    service = _get_service(plugin)
../ckan/ckan/plugins/core.py:284: in _get_service
    return plugin.load()(name=plugin_name)
/usr/local/lib/python3.10/site-packages/pkg_resources/__init__.py:2777: in load
    return self.resolve()
/usr/local/lib/python3.10/site-packages/pkg_resources/__init__.py:2783: in resolve
    module = __import__(self.module_name, fromlist=['__name__'], level=0)
../ckanext-validation/ckanext/validation/plugin/__init__.py:11: in <module>
    from ckanext.validation.logic import (
../ckanext-validation/ckanext/validation/logic.py:16: in <module>
    from ckanext.validation.jobs import run_validation_job
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

    # encoding: utf-8
    
    import logging
    import datetime
    import json
    import re
    
    import requests
    from sqlalchemy.orm.exc import NoResultFound
>   from frictionless import system, Resource, Package, Report, Schema, Dialect, Check, Checklist, Detector
E   ModuleNotFoundError: No module named 'frictionless'

../ckanext-validation/ckanext/validation/jobs.py:10: ModuleNotFoundError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:55,025 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:55,026 INFO  [ckan.model] Database initialised
2026-01-10 08:03:55,026 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:55,108 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____ ERROR at setup of TestValidators.test_organisation_id_exists_validator ____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:55,505 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:55,505 INFO  [ckan.model] Database initialised
2026-01-10 08:03:55,505 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:55,579 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:55,669 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:55,670 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:55,719 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_if_empty_guess_format[None-None-http://adr.local/resource/8919/demo.csv-text/csv] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:55,782 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:55,782 INFO  [ckan.model] Database initialised
2026-01-10 08:03:55,782 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:55,860 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:55,947 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:55,948 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:55,991 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_if_empty_guess_format[None-None-http://adr.local/resource/8919/demo.geojson-application/geo+json] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:56,050 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:56,050 INFO  [ckan.model] Database initialised
2026-01-10 08:03:56,050 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:56,120 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:56,205 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:56,205 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:56,256 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_if_empty_guess_format[None-None-http://adr.local/resource/8919/demo.pjnz-application/pjnz] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:56,323 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:56,323 INFO  [ckan.model] Database initialised
2026-01-10 08:03:56,323 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:56,396 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:56,473 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:56,474 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:56,524 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_if_empty_guess_format[text/csv-8919-http://adr.local/resource/89192/demo.xlsx-text/csv] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:56,638 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:56,638 INFO  [ckan.model] Database initialised
2026-01-10 08:03:56,638 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:56,718 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:56,798 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:56,799 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:56,845 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_if_empty_guess_format[None-8919-http://adr.local/resource/8919/demo.csv-None] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:56,900 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:56,900 INFO  [ckan.model] Database initialised
2026-01-10 08:03:56,900 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:56,980 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:57,061 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:57,062 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:57,110 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context0-result0] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:57,172 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:57,172 INFO  [ckan.model] Database initialised
2026-01-10 08:03:57,172 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:57,246 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:57,334 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:57,335 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:57,380 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context1-result1] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:57,494 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:57,494 INFO  [ckan.model] Database initialised
2026-01-10 08:03:57,494 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:57,575 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:57,656 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:57,657 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:57,707 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context2-result2] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:57,764 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:57,764 INFO  [ckan.model] Database initialised
2026-01-10 08:03:57,764 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:57,843 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:57,930 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:57,931 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:57,975 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context3-result3] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:58,045 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:58,045 INFO  [ckan.model] Database initialised
2026-01-10 08:03:58,045 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:58,116 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:58,200 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:58,201 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:58,247 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context4-result4] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:58,362 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:58,362 INFO  [ckan.model] Database initialised
2026-01-10 08:03:58,362 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:58,439 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:58,521 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:58,522 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:58,571 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context5-result5] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:58,633 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:58,633 INFO  [ckan.model] Database initialised
2026-01-10 08:03:58,633 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:58,718 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:58,801 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:58,802 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:58,845 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context6-result6] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:58,912 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:58,912 INFO  [ckan.model] Database initialised
2026-01-10 08:03:58,912 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:58,996 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:59,084 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:59,085 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:59,133 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context7-result7] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:59,243 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:59,243 INFO  [ckan.model] Database initialised
2026-01-10 08:03:59,243 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:59,324 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:59,409 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:59,410 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:59,463 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context8-result8] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:59,533 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:59,533 INFO  [ckan.model] Database initialised
2026-01-10 08:03:59,533 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:59,619 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:59,698 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:59,699 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:59,747 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_read_only_sets_default_value[context0-data0-false] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:59,812 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:59,812 INFO  [ckan.model] Database initialised
2026-01-10 08:03:59,813 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:59,890 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:59,985 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:59,986 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:04:00,028 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_read_only_sets_default_value[context1-data1-false] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...ds;ckanext-ytp_request;ckanext-scheming', 'TESTING': True, 'BABEL_DEFAULT_TIMEZONE': 'UTC', 'MAX_CONTENT_LENGTH': None}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:04:00,146 INFO  [ckan.model] Database table data deleted
2026-01-10 08:04:00,146 INFO  [ckan.model] Database initialised
2026-01-10 08:04:00,146 INFO  [ckan.model] Database rebuilt
2026-01-10 08:04:00,227 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:04:00,308 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:04:00,309 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:04:00,355 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
=================================== FAILURES ===================================
___________________ TestDatasetUnlock.test_metadata_updated ____________________

self = <ckanext.unaids.tests.test_actions_dataset_lock.TestDatasetUnlock object at 0x77c85062f730>
locked_dataset = {'author': None, 'author_email': None, 'creator_user_id': 'b04690aa-2e5b-454b-a51c-57b6cbc90f8d', 'groups': [], ...}

    def test_metadata_updated(self, locked_dataset):
>       call_action(
            'dataset_unlock',
            id=locked_dataset['id']
        )

ckanext/unaids/tests/test_actions_dataset_lock.py:66: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:128: in call_action
    return logic.get_action(action_name)(context, kwargs)
../ckan/ckan/logic/__init__.py:580: in wrapped
    result = _action(context, data_dict, **kw)
ckanext/unaids/actions.py:319: in dataset_unlock
    t.get_action("package_patch")(
../ckan/ckan/logic/__init__.py:580: in wrapped
    result = _action(context, data_dict, **kw)
../ckan/ckan/logic/action/patch.py:53: in package_patch
    return _get_action('package_update')(context, patched)
../ckan/ckan/logic/__init__.py:596: in wrapped
    signals.action_succeeded.send(
/usr/local/lib/python3.10/site-packages/blinker/base.py:279: in send
    result = receiver(sender, **kwargs)
../ckan/ckanext/activity/subscriptions.py:167: in package_changed
    _create_package_activity(
../ckan/ckanext/activity/subscriptions.py:82: in _create_package_activity
    user_obj = _get_user_or_raise(context["user"])
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

username = '127.0.0.1'

    def _get_user_or_raise(username: str) -> model.User:
        """Get user by username or raise standard exception.
        """
        if user := model.User.get(username):
            return user
    
>       raise tk.ValidationError({
            "user_id": ["User not found"]
        })
E       ckan.logic.ValidationError: None - {'user_id': ['User not found']}

../ckan/ckanext/activity/subscriptions.py:145: ValidationError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:22,228 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:22,228 INFO  [ckan.model] Database initialised
2026-01-10 08:03:22,228 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:22,310 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:22,401 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:22,409 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:22,410 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:22,452 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
--------------------------- Captured stdout teardown ---------------------------
2026-01-10 08:03:23,759 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
---------------------------- Captured log teardown -----------------------------
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____________________ TestDatasetUnlock.test_version_deleted ____________________

self = <ckanext.unaids.tests.test_actions_dataset_lock.TestDatasetUnlock object at 0x77c85062e740>
locked_dataset = {'author': None, 'author_email': None, 'creator_user_id': 'e321400d-4560-4e12-b58f-f09fdfa1fe47', 'groups': [], ...}

    def test_version_deleted(self, locked_dataset):
>       call_action(
            'dataset_unlock',
            id=locked_dataset['id']
        )

ckanext/unaids/tests/test_actions_dataset_lock.py:77: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:128: in call_action
    return logic.get_action(action_name)(context, kwargs)
../ckan/ckan/logic/__init__.py:580: in wrapped
    result = _action(context, data_dict, **kw)
ckanext/unaids/actions.py:319: in dataset_unlock
    t.get_action("package_patch")(
../ckan/ckan/logic/__init__.py:580: in wrapped
    result = _action(context, data_dict, **kw)
../ckan/ckan/logic/action/patch.py:53: in package_patch
    return _get_action('package_update')(context, patched)
../ckan/ckan/logic/__init__.py:596: in wrapped
    signals.action_succeeded.send(
/usr/local/lib/python3.10/site-packages/blinker/base.py:279: in send
    result = receiver(sender, **kwargs)
../ckan/ckanext/activity/subscriptions.py:167: in package_changed
    _create_package_activity(
../ckan/ckanext/activity/subscriptions.py:82: in _create_package_activity
    user_obj = _get_user_or_raise(context["user"])
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

username = '127.0.0.1'

    def _get_user_or_raise(username: str) -> model.User:
        """Get user by username or raise standard exception.
        """
        if user := model.User.get(username):
            return user
    
>       raise tk.ValidationError({
            "user_id": ["User not found"]
        })
E       ckan.logic.ValidationError: None - {'user_id': ['User not found']}

../ckan/ckanext/activity/subscriptions.py:145: ValidationError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:23,803 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:23,803 INFO  [ckan.model] Database initialised
2026-01-10 08:03:23,803 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:23,884 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:23,985 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:23,992 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:23,993 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:24,034 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
--------------------------- Captured stdout teardown ---------------------------
2026-01-10 08:03:25,378 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
---------------------------- Captured log teardown -----------------------------
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________________ TestDatasetUnlock.test_version_already_deleted ________________

self = <ckanext.unaids.tests.test_actions_dataset_lock.TestDatasetUnlock object at 0x77c85062dcf0>
locked_dataset = {'author': None, 'author_email': None, 'creator_user_id': 'e2e3becb-e844-4307-9e20-81eaab4ed411', 'groups': [], ...}

    def test_version_already_deleted(self, locked_dataset):
        versions = call_action('dataset_version_list', dataset_id=locked_dataset['id'])
        call_action(
            "version_delete",
            version_id=versions[-1]["id"],
        )
        with pytest.raises(toolkit.ObjectNotFound):
>           call_action(
                'dataset_unlock',
                id=locked_dataset['id']
            )

ckanext/unaids/tests/test_actions_dataset_lock.py:91: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:128: in call_action
    return logic.get_action(action_name)(context, kwargs)
../ckan/ckan/logic/__init__.py:580: in wrapped
    result = _action(context, data_dict, **kw)
ckanext/unaids/actions.py:319: in dataset_unlock
    t.get_action("package_patch")(
../ckan/ckan/logic/__init__.py:580: in wrapped
    result = _action(context, data_dict, **kw)
../ckan/ckan/logic/action/patch.py:53: in package_patch
    return _get_action('package_update')(context, patched)
../ckan/ckan/logic/__init__.py:596: in wrapped
    signals.action_succeeded.send(
/usr/local/lib/python3.10/site-packages/blinker/base.py:279: in send
    result = receiver(sender, **kwargs)
../ckan/ckanext/activity/subscriptions.py:167: in package_changed
    _create_package_activity(
../ckan/ckanext/activity/subscriptions.py:82: in _create_package_activity
    user_obj = _get_user_or_raise(context["user"])
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

username = '127.0.0.1'

    def _get_user_or_raise(username: str) -> model.User:
        """Get user by username or raise standard exception.
        """
        if user := model.User.get(username):
            return user
    
>       raise tk.ValidationError({
            "user_id": ["User not found"]
        })
E       ckan.logic.ValidationError: None - {'user_id': ['User not found']}

../ckan/ckanext/activity/subscriptions.py:145: ValidationError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:25,421 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:25,421 INFO  [ckan.model] Database initialised
2026-01-10 08:03:25,422 INFO  [ckan.model] Database rebuilt
2026-01-10 08:03:25,496 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 08:03:25,593 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 08:03:25,600 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 08:03:25,601 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 08:03:25,638 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
--------------------------- Captured stdout teardown ---------------------------
2026-01-10 08:03:26,962 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
---------------------------- Captured log teardown -----------------------------
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_______________ TestAutoPopulateDataDictionaries.test_no_schema ________________

self = <ckanext.unaids.tests.test_logic.TestAutoPopulateDataDictionaries object at 0x77c8505b5990>
mocker = <pytest_mock.plugin.MockerFixture object at 0x77c84f75e290>

    def test_no_schema(self, mocker):
        context = {}
>       dataset = factories.Dataset()

ckanext/unaids/tests/test_logic.py:86: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/usr/local/lib/python3.10/site-packages/factory/base.py:40: in __call__
    return cls.create(**kwargs)
/usr/local/lib/python3.10/site-packages/factory/base.py:528: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
/usr/local/lib/python3.10/site-packages/factory/alchemy.py:60: in _generate
    return super()._generate(strategy, params)
/usr/local/lib/python3.10/site-packages/factory/base.py:465: in _generate
    return step.build()
/usr/local/lib/python3.10/site-packages/factory/builder.py:274: in build
    instance = self.factory_meta.instantiate(
/usr/local/lib/python3.10/site-packages/factory/base.py:317: in instantiate
    return self.factory._create(model, *args, **kwargs)
../ckan/ckan/tests/factories.py:220: in _create
    return cls.api_create(kwargs)
../ckan/ckan/tests/factories.py:203: in api_create
    result = helpers.call_action(cls._meta.action, **data_dict)
../ckan/ckan/tests/helpers.py:128: in call_action
    return logic.get_action(action_name)(context, kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

context = {'ignore_auth': True, 'user': 'test.ckan.net'}
data_dict = {'name': 'dataset-uike-6030-xyol', 'notes': 'Face dark nice factor cause next.', 'title': 'Government trial them claim despite.'}

    def package_create(
            context: Context, data_dict: DataDict) -> ActionResult.PackageCreate:
        '''Create a new dataset (package).
    
        You must be authorized to create new datasets. If you specify any groups
        for the new dataset, you must also be authorized to edit these groups.
    
        Plugins may change the parameters of this function depending on the value
        of the ``type`` parameter, see the
        :py:class:`~ckan.plugins.interfaces.IDatasetForm` plugin interface.
    
        :param name: the name of the new dataset, must be between 2 and 100
            characters long and contain only lowercase alphanumeric characters,
            ``-`` and ``_``, e.g. ``'warandpeace'``
        :type name: string
        :param title: the title of the dataset (optional, default: same as
            ``name``)
        :type title: string
        :param private: If ``True`` creates a private dataset
        :type private: bool
        :param author: the name of the dataset's author (optional)
        :type author: string
        :param author_email: the email address of the dataset's author (optional)
        :type author_email: string
        :param maintainer: the name of the dataset's maintainer (optional)
        :type maintainer: string
        :param maintainer_email: the email address of the dataset's maintainer
            (optional)
        :type maintainer_email: string
        :param license_id: the id of the dataset's license, see
            :py:func:`~ckan.logic.action.get.license_list` for available values
            (optional)
        :type license_id: license id string
        :param notes: a description of the dataset (optional)
        :type notes: string
        :param url: a URL for the dataset's source (optional)
        :type url: string
        :param version: (optional)
        :type version: string, no longer than 100 characters
        :param state: the current state of the dataset, e.g. ``'active'`` or
            ``'deleted'``, only active datasets show up in search results and
            other lists of datasets, this parameter will be ignored if you are not
            authorized to change the state of the dataset (optional, default:
            ``'active'``)
        :type state: string
        :param type: the type of the dataset (optional),
            :py:class:`~ckan.plugins.interfaces.IDatasetForm` plugins
            associate themselves with different dataset types and provide custom
            dataset handling behaviour for these types
        :type type: string
        :param resources: the dataset's resources, see
            :py:func:`resource_create` for the format of resource dictionaries
            (optional)
        :type resources: list of resource dictionaries
        :param tags: the dataset's tags, see :py:func:`tag_create` for the format
            of tag dictionaries (optional)
        :type tags: list of tag dictionaries
        :param extras: the dataset's extras (optional), extras are arbitrary
            (key: value) metadata items that can be added to datasets, each extra
            dictionary should have keys ``'key'`` (a string), ``'value'`` (a
            string)
        :type extras: list of dataset extra dictionaries
        :param plugin_data: private package data belonging to plugins.
            Only sysadmin users may set this value. It should be a dict that can
            be dumped into JSON, and plugins should namespace their data with the
            plugin name to avoid collisions with other plugins, eg::
    
                {
                    "name": "test-dataset",
                    "plugin_data": {
                        "plugin1": {"key1": "value1"},
                        "plugin2": {"key2": "value2"}
                    }
                }
        :type plugin_data: dict
        :param relationships_as_object: see :py:func:`package_relationship_create`
            for the format of relationship dictionaries (optional)
        :type relationships_as_object: list of relationship dictionaries
        :param relationships_as_subject: see :py:func:`package_relationship_create`
            for the format of relationship dictionaries (optional)
        :type relationships_as_subject: list of relationship dictionaries
        :param groups: the groups to which the dataset belongs (optional), each
            group dictionary should have one or more of the following keys which
            identify an existing group:
            ``'id'`` (the id of the group, string), or ``'name'`` (the name of the
            group, string),  to see which groups exist
            call :py:func:`~ckan.logic.action.get.group_list`
        :type groups: list of dictionaries
        :param owner_org: the id of the dataset's owning organization, see
            :py:func:`~ckan.logic.action.get.organization_list` or
            :py:func:`~ckan.logic.action.get.organization_list_for_user` for
            available values. This parameter can be made optional if the config
            option :ref:`ckan.auth.create_unowned_dataset` is set to ``True``.
        :type owner_org: string
    
        :returns: the newly created dataset (unless 'return_id_only' is set to True
                  in the context, in which case just the dataset id will
                  be returned)
        :rtype: dictionary
    
        '''
>       model = context['model']
E       KeyError: 'model'

../ckan/ckan/logic/action/create.py:156: KeyError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:53,820 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:53,820 INFO  [ckan.model] Database initialised
2026-01-10 08:03:53,820 INFO  [ckan.model] Database rebuilt
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
_____________ TestAutoPopulateDataDictionaries.test_missing_schema _____________

self = <ckanext.unaids.tests.test_logic.TestAutoPopulateDataDictionaries object at 0x77c8505b5bd0>
mocker = <pytest_mock.plugin.MockerFixture object at 0x77c84f8c3b50>

    def test_missing_schema(self, mocker):
        context = {}
>       dataset = factories.Dataset()

ckanext/unaids/tests/test_logic.py:98: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/usr/local/lib/python3.10/site-packages/factory/base.py:40: in __call__
    return cls.create(**kwargs)
/usr/local/lib/python3.10/site-packages/factory/base.py:528: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
/usr/local/lib/python3.10/site-packages/factory/alchemy.py:60: in _generate
    return super()._generate(strategy, params)
/usr/local/lib/python3.10/site-packages/factory/base.py:465: in _generate
    return step.build()
/usr/local/lib/python3.10/site-packages/factory/builder.py:274: in build
    instance = self.factory_meta.instantiate(
/usr/local/lib/python3.10/site-packages/factory/base.py:317: in instantiate
    return self.factory._create(model, *args, **kwargs)
../ckan/ckan/tests/factories.py:220: in _create
    return cls.api_create(kwargs)
../ckan/ckan/tests/factories.py:203: in api_create
    result = helpers.call_action(cls._meta.action, **data_dict)
../ckan/ckan/tests/helpers.py:128: in call_action
    return logic.get_action(action_name)(context, kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

context = {'ignore_auth': True, 'user': 'test.ckan.net'}
data_dict = {'name': 'dataset-mlsb-1361-mwaj', 'notes': 'Wife nature popular. Energy until nice.', 'title': 'Customer red reduce wrong human.'}

    def package_create(
            context: Context, data_dict: DataDict) -> ActionResult.PackageCreate:
        '''Create a new dataset (package).
    
        You must be authorized to create new datasets. If you specify any groups
        for the new dataset, you must also be authorized to edit these groups.
    
        Plugins may change the parameters of this function depending on the value
        of the ``type`` parameter, see the
        :py:class:`~ckan.plugins.interfaces.IDatasetForm` plugin interface.
    
        :param name: the name of the new dataset, must be between 2 and 100
            characters long and contain only lowercase alphanumeric characters,
            ``-`` and ``_``, e.g. ``'warandpeace'``
        :type name: string
        :param title: the title of the dataset (optional, default: same as
            ``name``)
        :type title: string
        :param private: If ``True`` creates a private dataset
        :type private: bool
        :param author: the name of the dataset's author (optional)
        :type author: string
        :param author_email: the email address of the dataset's author (optional)
        :type author_email: string
        :param maintainer: the name of the dataset's maintainer (optional)
        :type maintainer: string
        :param maintainer_email: the email address of the dataset's maintainer
            (optional)
        :type maintainer_email: string
        :param license_id: the id of the dataset's license, see
            :py:func:`~ckan.logic.action.get.license_list` for available values
            (optional)
        :type license_id: license id string
        :param notes: a description of the dataset (optional)
        :type notes: string
        :param url: a URL for the dataset's source (optional)
        :type url: string
        :param version: (optional)
        :type version: string, no longer than 100 characters
        :param state: the current state of the dataset, e.g. ``'active'`` or
            ``'deleted'``, only active datasets show up in search results and
            other lists of datasets, this parameter will be ignored if you are not
            authorized to change the state of the dataset (optional, default:
            ``'active'``)
        :type state: string
        :param type: the type of the dataset (optional),
            :py:class:`~ckan.plugins.interfaces.IDatasetForm` plugins
            associate themselves with different dataset types and provide custom
            dataset handling behaviour for these types
        :type type: string
        :param resources: the dataset's resources, see
            :py:func:`resource_create` for the format of resource dictionaries
            (optional)
        :type resources: list of resource dictionaries
        :param tags: the dataset's tags, see :py:func:`tag_create` for the format
            of tag dictionaries (optional)
        :type tags: list of tag dictionaries
        :param extras: the dataset's extras (optional), extras are arbitrary
            (key: value) metadata items that can be added to datasets, each extra
            dictionary should have keys ``'key'`` (a string), ``'value'`` (a
            string)
        :type extras: list of dataset extra dictionaries
        :param plugin_data: private package data belonging to plugins.
            Only sysadmin users may set this value. It should be a dict that can
            be dumped into JSON, and plugins should namespace their data with the
            plugin name to avoid collisions with other plugins, eg::
    
                {
                    "name": "test-dataset",
                    "plugin_data": {
                        "plugin1": {"key1": "value1"},
                        "plugin2": {"key2": "value2"}
                    }
                }
        :type plugin_data: dict
        :param relationships_as_object: see :py:func:`package_relationship_create`
            for the format of relationship dictionaries (optional)
        :type relationships_as_object: list of relationship dictionaries
        :param relationships_as_subject: see :py:func:`package_relationship_create`
            for the format of relationship dictionaries (optional)
        :type relationships_as_subject: list of relationship dictionaries
        :param groups: the groups to which the dataset belongs (optional), each
            group dictionary should have one or more of the following keys which
            identify an existing group:
            ``'id'`` (the id of the group, string), or ``'name'`` (the name of the
            group, string),  to see which groups exist
            call :py:func:`~ckan.logic.action.get.group_list`
        :type groups: list of dictionaries
        :param owner_org: the id of the dataset's owning organization, see
            :py:func:`~ckan.logic.action.get.organization_list` or
            :py:func:`~ckan.logic.action.get.organization_list_for_user` for
            available values. This parameter can be made optional if the config
            option :ref:`ckan.auth.create_unowned_dataset` is set to ``True``.
        :type owner_org: string
    
        :returns: the newly created dataset (unless 'return_id_only' is set to True
                  in the context, in which case just the dataset id will
                  be returned)
        :rtype: dictionary
    
        '''
>       model = context['model']
E       KeyError: 'model'

../ckan/ckan/logic/action/create.py:156: KeyError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:53,913 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:53,913 INFO  [ckan.model] Database initialised
2026-01-10 08:03:53,913 INFO  [ckan.model] Database rebuilt
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
_____________ TestAutoPopulateDataDictionaries.test_simple_schema ______________

self = <ckanext.unaids.tests.test_logic.TestAutoPopulateDataDictionaries object at 0x77c8505b5e10>
mocker = <pytest_mock.plugin.MockerFixture object at 0x77c84f732080>

    def test_simple_schema(self, mocker):
        context = {}
>       dataset = factories.Dataset()

ckanext/unaids/tests/test_logic.py:113: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/usr/local/lib/python3.10/site-packages/factory/base.py:40: in __call__
    return cls.create(**kwargs)
/usr/local/lib/python3.10/site-packages/factory/base.py:528: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
/usr/local/lib/python3.10/site-packages/factory/alchemy.py:60: in _generate
    return super()._generate(strategy, params)
/usr/local/lib/python3.10/site-packages/factory/base.py:465: in _generate
    return step.build()
/usr/local/lib/python3.10/site-packages/factory/builder.py:274: in build
    instance = self.factory_meta.instantiate(
/usr/local/lib/python3.10/site-packages/factory/base.py:317: in instantiate
    return self.factory._create(model, *args, **kwargs)
../ckan/ckan/tests/factories.py:220: in _create
    return cls.api_create(kwargs)
../ckan/ckan/tests/factories.py:203: in api_create
    result = helpers.call_action(cls._meta.action, **data_dict)
../ckan/ckan/tests/helpers.py:128: in call_action
    return logic.get_action(action_name)(context, kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

context = {'ignore_auth': True, 'user': 'test.ckan.net'}
data_dict = {'name': 'dataset-opvf-2340-kbgd', 'notes': 'Billion prepare church boy let.', 'title': 'Former foreign financial east.'}

    def package_create(
            context: Context, data_dict: DataDict) -> ActionResult.PackageCreate:
        '''Create a new dataset (package).
    
        You must be authorized to create new datasets. If you specify any groups
        for the new dataset, you must also be authorized to edit these groups.
    
        Plugins may change the parameters of this function depending on the value
        of the ``type`` parameter, see the
        :py:class:`~ckan.plugins.interfaces.IDatasetForm` plugin interface.
    
        :param name: the name of the new dataset, must be between 2 and 100
            characters long and contain only lowercase alphanumeric characters,
            ``-`` and ``_``, e.g. ``'warandpeace'``
        :type name: string
        :param title: the title of the dataset (optional, default: same as
            ``name``)
        :type title: string
        :param private: If ``True`` creates a private dataset
        :type private: bool
        :param author: the name of the dataset's author (optional)
        :type author: string
        :param author_email: the email address of the dataset's author (optional)
        :type author_email: string
        :param maintainer: the name of the dataset's maintainer (optional)
        :type maintainer: string
        :param maintainer_email: the email address of the dataset's maintainer
            (optional)
        :type maintainer_email: string
        :param license_id: the id of the dataset's license, see
            :py:func:`~ckan.logic.action.get.license_list` for available values
            (optional)
        :type license_id: license id string
        :param notes: a description of the dataset (optional)
        :type notes: string
        :param url: a URL for the dataset's source (optional)
        :type url: string
        :param version: (optional)
        :type version: string, no longer than 100 characters
        :param state: the current state of the dataset, e.g. ``'active'`` or
            ``'deleted'``, only active datasets show up in search results and
            other lists of datasets, this parameter will be ignored if you are not
            authorized to change the state of the dataset (optional, default:
            ``'active'``)
        :type state: string
        :param type: the type of the dataset (optional),
            :py:class:`~ckan.plugins.interfaces.IDatasetForm` plugins
            associate themselves with different dataset types and provide custom
            dataset handling behaviour for these types
        :type type: string
        :param resources: the dataset's resources, see
            :py:func:`resource_create` for the format of resource dictionaries
            (optional)
        :type resources: list of resource dictionaries
        :param tags: the dataset's tags, see :py:func:`tag_create` for the format
            of tag dictionaries (optional)
        :type tags: list of tag dictionaries
        :param extras: the dataset's extras (optional), extras are arbitrary
            (key: value) metadata items that can be added to datasets, each extra
            dictionary should have keys ``'key'`` (a string), ``'value'`` (a
            string)
        :type extras: list of dataset extra dictionaries
        :param plugin_data: private package data belonging to plugins.
            Only sysadmin users may set this value. It should be a dict that can
            be dumped into JSON, and plugins should namespace their data with the
            plugin name to avoid collisions with other plugins, eg::
    
                {
                    "name": "test-dataset",
                    "plugin_data": {
                        "plugin1": {"key1": "value1"},
                        "plugin2": {"key2": "value2"}
                    }
                }
        :type plugin_data: dict
        :param relationships_as_object: see :py:func:`package_relationship_create`
            for the format of relationship dictionaries (optional)
        :type relationships_as_object: list of relationship dictionaries
        :param relationships_as_subject: see :py:func:`package_relationship_create`
            for the format of relationship dictionaries (optional)
        :type relationships_as_subject: list of relationship dictionaries
        :param groups: the groups to which the dataset belongs (optional), each
            group dictionary should have one or more of the following keys which
            identify an existing group:
            ``'id'`` (the id of the group, string), or ``'name'`` (the name of the
            group, string),  to see which groups exist
            call :py:func:`~ckan.logic.action.get.group_list`
        :type groups: list of dictionaries
        :param owner_org: the id of the dataset's owning organization, see
            :py:func:`~ckan.logic.action.get.organization_list` or
            :py:func:`~ckan.logic.action.get.organization_list_for_user` for
            available values. This parameter can be made optional if the config
            option :ref:`ckan.auth.create_unowned_dataset` is set to ``True``.
        :type owner_org: string
    
        :returns: the newly created dataset (unless 'return_id_only' is set to True
                  in the context, in which case just the dataset id will
                  be returned)
        :rtype: dictionary
    
        '''
>       model = context['model']
E       KeyError: 'model'

../ckan/ckan/logic/action/create.py:156: KeyError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 08:03:54,014 INFO  [ckan.model] Database table data deleted
2026-01-10 08:03:54,014 INFO  [ckan.model] Database initialised
2026-01-10 08:03:54,014 INFO  [ckan.model] Database rebuilt
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt

---------- coverage: platform linux, python 3.10.19-final-0 ----------
Name                                                        Stmts   Miss  Cover
-------------------------------------------------------------------------------
ckanext/unaids/__init__.py                                      6      3    50%
ckanext/unaids/actions.py                                     167     93    44%
ckanext/unaids/auth.py                                         24     16    33%
ckanext/unaids/auth_logic.py                                   88     40    55%
ckanext/unaids/blueprints/__init__.py                          10      0   100%
ckanext/unaids/blueprints/dataset_lock.py                      21     12    43%
ckanext/unaids/blueprints/login_register_catch.py               9      1    89%
ckanext/unaids/blueprints/members_list.py                      27     18    33%
ckanext/unaids/blueprints/profile_editor_data_receiver.py      11      4    64%
ckanext/unaids/blueprints/svg_map_options.py                   26     14    46%
ckanext/unaids/blueprints/unaids_dataset_releases.py           94     63    33%
ckanext/unaids/blueprints/unaids_dataset_transfer.py           25     12    52%
ckanext/unaids/blueprints/user_info_blueprint.py               20      9    55%
ckanext/unaids/blueprints/validate_user_affiliation.py         27     14    48%
ckanext/unaids/command.py                                      15      7    53%
ckanext/unaids/custom_user_profile/actions.py                  83     64    23%
ckanext/unaids/custom_user_profile/logic.py                    43     34    21%
ckanext/unaids/dataset_transfer/__init__.py                     6      3    50%
ckanext/unaids/dataset_transfer/logic.py                       38     28    26%
ckanext/unaids/dataset_transfer/model.py                       25      0   100%
ckanext/unaids/helpers.py                                     188    143    24%
ckanext/unaids/licenses.py                                      9      1    89%
ckanext/unaids/logic.py                                        46     39    15%
ckanext/unaids/plugin.py                                      203     96    53%
ckanext/unaids/tests/__init__.py                               21      9    57%
ckanext/unaids/tests/conftest.py                               39      7    82%
ckanext/unaids/tests/test_actions.py                          108     62    43%
ckanext/unaids/tests/test_actions_dataset_lock.py              52      4    92%
ckanext/unaids/tests/test_actions_show_for_release.py          44     30    32%
ckanext/unaids/tests/test_auth.py                              43     25    42%
ckanext/unaids/tests/test_auth_logic.py                       186     88    53%
ckanext/unaids/tests/test_blueprints.py                        45     23    49%
ckanext/unaids/tests/test_dataset_releases.py                 185    136    26%
ckanext/unaids/tests/test_dataset_transfer.py                  95     73    23%
ckanext/unaids/tests/test_giftless_backend.py                  35     20    43%
ckanext/unaids/tests/test_helpers.py                           14      5    64%
ckanext/unaids/tests/test_logic.py                             63     38    40%
ckanext/unaids/tests/test_plugin.py                           109     27    75%
ckanext/unaids/tests/test_scheming_schemas/__init__.py          0      0   100%
ckanext/unaids/tests/test_validators.py                        39     19    51%
ckanext/unaids/validators.py                                   38     14    63%
-------------------------------------------------------------------------------
TOTAL                                                        2327   1294    44%

=========================== short test summary info ============================
FAILED ckanext/unaids/tests/test_actions_dataset_lock.py::TestDatasetUnlock::test_metadata_updated
FAILED ckanext/unaids/tests/test_actions_dataset_lock.py::TestDatasetUnlock::test_version_deleted
FAILED ckanext/unaids/tests/test_actions_dataset_lock.py::TestDatasetUnlock::test_version_already_deleted
FAILED ckanext/unaids/tests/test_logic.py::TestAutoPopulateDataDictionaries::test_no_schema
FAILED ckanext/unaids/tests/test_logic.py::TestAutoPopulateDataDictionaries::test_missing_schema
FAILED ckanext/unaids/tests/test_logic.py::TestAutoPopulateDataDictionaries::test_simple_schema
ERROR ckanext/unaids/tests/test_actions.py::TestGetTableSchema::test_schema_returned_successfully
ERROR ckanext/unaids/tests/test_actions.py::TestGetTableSchema::test_empty_dict_returned_for_no_schema
ERROR ckanext/unaids/tests/test_actions.py::TestPackageActivityList::test_package_activity_list_containes_releases_names
ERROR ckanext/unaids/tests/test_actions.py::TestFormatGuess::test_format_guess_csv[art.csv-text/csv-CSV]
ERROR ckanext/unaids/tests/test_actions.py::TestFormatGuess::test_format_guess_csv[anc.xls-application/vnd.ms-excel-XLS]
ERROR ckanext/unaids/tests/test_actions.py::TestFormatGuess::test_format_guess_csv[country_regions.geojson-application/geo+json-GeoJSON]
ERROR ckanext/unaids/tests/test_actions.py::TestFormatGuess::test_format_guess_csv[spectrum_file.pjnz-application/pjnz-PJNZ]
ERROR ckanext/unaids/tests/test_actions.py::TestFormatGuess::test_format_guess_csv[no_file_extension-None-None]
ERROR ckanext/unaids/tests/test_actions.py::TestFormatGuess::test_format_guess_csv[-None-None]
ERROR ckanext/unaids/tests/test_actions.py::TestUserShowMe::test_no_user - ck...
ERROR ckanext/unaids/tests/test_actions.py::TestUserShowMe::test_user - ckan....
ERROR ckanext/unaids/tests/test_actions.py::TestPopulateDataDictionary::test_expected_use
ERROR ckanext/unaids/tests/test_actions.py::TestUserAffiliation::test_user_show_with_affiliation
ERROR ckanext/unaids/tests/test_actions.py::TestUserAffiliation::test_user_list_all_fields_with_affiliation
ERROR ckanext/unaids/tests/test_actions.py::TestUserAffiliation::test_user_list_usernames
ERROR ckanext/unaids/tests/test_actions.py::TestPackageCreate::test_should_complain_with_exception_when_dataset_type_invalid
ERROR ckanext/unaids/tests/test_actions.py::TestPackageCreate::test_create_dataset_without_type_creates_one_with_default_type_of_dataset
ERROR ckanext/unaids/tests/test_actions.py::TestPackageCreate::test_create_dataset_with_valid_type
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_displays_release[id]
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_displays_release[name]
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_raises_when_incorrect_release
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_returns_correct_download_links
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_only_returns_updated_resource_download_links_for_uploads
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_auth[admin-True]
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_auth[editor-True]
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_auth[member-True]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_unaids_organization_update_valid
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_unaids_organization_update_invalid
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_dataset_lock[True-outcome0]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_dataset_lock[False-outcome1]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[False-False-False-outcome0]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[False-True-False-outcome1]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[False-False-True-outcome2]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[False-True-True-outcome3]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[True-False-False-outcome4]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[True-True-False-outcome5]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[True-False-True-outcome6]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[True-True-True-outcome7]
ERROR ckanext/unaids/tests/test_auth_logic.py::TestVerifyRequiredScope::test_should_pass_when_scope_is_right
ERROR ckanext/unaids/tests/test_auth_logic.py::TestVerifyRequiredScope::test_should_complain_when_scope_is_none
ERROR ckanext/unaids/tests/test_auth_logic.py::TestVerifyRequiredScope::test_should_complain_when_scope_is_incorrect
ERROR ckanext/unaids/tests/test_auth_logic.py::TestAccessTokenPresentAndValidAndUserAuthorized::test_should_set_user_when_token_correct_and_user_authorized
ERROR ckanext/unaids/tests/test_auth_logic.py::TestAccessTokenPresentAndValidAndUserAuthorized::test_should_return_false_when_no_token
ERROR ckanext/unaids/tests/test_auth_logic.py::TestAccessTokenPresentAndValidAndUserAuthorized::test_should_throw_when_subject_of_different_origin_than_auth0
ERROR ckanext/unaids/tests/test_auth_logic.py::TestValidateAndDecodeToken::test_should_validate_and_decode_token
ERROR ckanext/unaids/tests/test_auth_logic.py::TestValidateAndDecodeToken::test_should_throw_when_no_matching_rsa_key
ERROR ckanext/unaids/tests/test_auth_logic.py::TestValidateAndDecodeToken::test_should_throw_when_token_expired
ERROR ckanext/unaids/tests/test_auth_logic.py::TestValidateAndDecodeToken::test_should_throw_when_incorrect_claims
ERROR ckanext/unaids/tests/test_auth_logic.py::TestValidateAndDecodeToken::test_should_throw_when_general_exception_thrown
ERROR ckanext/unaids/tests/test_auth_logic.py::TestRegressionOAuth2PluginDoesntPreventVanillaCkanAuthentication::test_using_api_key
ERROR ckanext/unaids/tests/test_auth_logic.py::TestRegressionOAuth2PluginDoesntPreventVanillaCkanAuthentication::test_using_api_token
ERROR ckanext/unaids/tests/test_blueprints.py::TestValidateUserProfileBlueprint::test_annonymous_access_to_index_page
ERROR ckanext/unaids/tests/test_blueprints.py::TestMemberLists::test_org_member_download_200_ok
ERROR ckanext/unaids/tests/test_blueprints.py::TestMemberLists::test_org_member_download_expected_columns
ERROR ckanext/unaids/tests/test_blueprints.py::TestMemberLists::test_org_member_download_expected_emails
ERROR ckanext/unaids/tests/test_blueprints.py::TestMemberLists::test_org_member_download_403
ERROR ckanext/unaids/tests/test_blueprints.py::TestMemberLists::test_org_member_download_404
ERROR ckanext/unaids/tests/test_blueprints.py::TestMemberLists::test_org_member_download_anonymous_access
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseCreateAndEdit::test_create_with_valid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseCreateAndEdit::test_create_with_invalid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseCreateAndEdit::test_create_with_existing_activity_id
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseCreateAndEdit::test_create_with_existing_release_name
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseCreateAndEdit::test_edit_with_valid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseCreateAndEdit::test_edit_with_invalid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseDelete::test_delete_with_valid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseDelete::test_delete_with_invalid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseRestore::test_restore_with_valid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseRestore::test_restore_with_invalid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseListView::test_no_releases_created
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseListView::test_releases_are_listed_to_owners
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseListView::test_releases_are_listed_to_outsiders
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseListView::test_add_release_button_is_shown_to_owners
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseListView::test_add_release_button_is_not_shown_to_outsiders
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetRead::test_activity_id_query_param
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetRead::test_when_no_release_for_dataset
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetRead::test_when_on_latest_version_of_dataset_has_release
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_collaborator_transfering_dataset
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_get_org_admins_with_email_addresses_two_admins
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_get_org_admins_with_email_addresses_one_admin
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_get_org_admins_with_email_addresses_no_admins
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_get_org_admins_with_email_addresses_two_orgs
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_get_org_admins_with_no_users_in_org
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_send_dataset_transfer_emails
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_send_dataset_transfer_email_subject
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_send_dataset_transfer_emails_errors
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_dataset_transfer_request
ERROR ckanext/unaids/tests/test_giftless_backend.py::TestResourceUrlEncoding::test_resource_url_encoding_test
ERROR ckanext/unaids/tests/test_helpers.py::TestDatasetLockHelper::test_dataset_lockable
ERROR ckanext/unaids/tests/test_helpers.py::TestDatasetLockHelper::test_dataset_not_lockable
ERROR ckanext/unaids/tests/test_helpers.py::TestDatasetLockHelper::test_dataset_not_found
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[validates if all upload fields correct]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[validates if no upload fields present]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[validates if all fields are present but empty]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if some fields missing and others set to empty string]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if size None]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if only size present]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if sha256 empty string]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if sha256 None]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if only sha256 present]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if invalid sha256]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if lfs_prefix empty string]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if lfs_prefix None]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if only lfs_prefix present]
ERROR ckanext/unaids/tests/test_logic.py::test_update_filename_in_upload_resource_url
ERROR ckanext/unaids/tests/test_logic.py::test_update_filename_in_link_resource_url[http url]
ERROR ckanext/unaids/tests/test_logic.py::test_update_filename_in_link_resource_url[https url]
ERROR ckanext/unaids/tests/test_logic.py::test_update_filename_in_link_resource_url[url with path]
ERROR ckanext/unaids/tests/test_plugin.py::TestPlugin::test_geojson_format_guessed_correctly
ERROR ckanext/unaids/tests/test_plugin.py::TestPlugin::test_blob_storage_validator_is_used_during_resource_actions
ERROR ckanext/unaids/tests/test_plugin.py::TestValidatePackage::test_validate_package
ERROR ckanext/unaids/tests/test_plugin.py::TestValidatePackage::test_metadata_change_does_not_validate_package
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_organisation_id_exists_validator
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_if_empty_guess_format[None-None-http://adr.local/resource/8919/demo.csv-text/csv]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_if_empty_guess_format[None-None-http://adr.local/resource/8919/demo.geojson-application/geo+json]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_if_empty_guess_format[None-None-http://adr.local/resource/8919/demo.pjnz-application/pjnz]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_if_empty_guess_format[text/csv-8919-http://adr.local/resource/89192/demo.xlsx-text/csv]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_if_empty_guess_format[None-8919-http://adr.local/resource/8919/demo.csv-None]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context0-result0]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context1-result1]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context2-result2]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context3-result3]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context4-result4]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context5-result5]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context6-result6]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context7-result7]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context8-result8]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_sets_default_value[context0-data0-false]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_sets_default_value[context1-data1-false]
===== 6 failed, 27 passed, 1 skipped, 1038 warnings, 128 errors in 49.57s ======
