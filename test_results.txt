2026-01-10 07:49:09,423 INFO  [ckan.cli] Using configuration file /srv/app/src/ckanext-unaids/test.ini
2026-01-10 07:49:09,423 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 07:49:09,548 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:09,688 WARNI [ckan.config.middleware.flask_app] Extensions are excluded from CSRF protection! We allow extensions to run without CSRF protection but it will be forced future releases. Read the documentation for more information on how to add CSRF protection to your extension.
collected 162 items

ckanext/unaids/tests/test_actions.py EEEEEEEEEEEEEEEEEE                  [ 11%]
ckanext/unaids/tests/test_actions_dataset_lock.py ...FFF                 [ 14%]
ckanext/unaids/tests/test_actions_show_for_release.py EEEEEEEE           [ 19%]
ckanext/unaids/tests/test_auth.py EEEEEEEEEEEE                           [ 27%]
ckanext/unaids/tests/test_auth_logic.py .........EEEEEE.....EEEEEEE      [ 43%]
ckanext/unaids/tests/test_blueprints.py EEEEEEE                          [ 48%]
ckanext/unaids/tests/test_dataset_releases.py EEEEEEEEEEEEEEEEEE         [ 59%]
ckanext/unaids/tests/test_dataset_transfer.py EEEEEEEEEE                 [ 65%]
ckanext/unaids/tests/test_giftless_backend.py sE                         [ 66%]
ckanext/unaids/tests/test_helpers.py EEE                                 [ 68%]
ckanext/unaids/tests/test_logic.py EEEEEEEEEEEEEEEEEFFF                  [ 80%]
ckanext/unaids/tests/test_plugin.py EEEE..........                       [ 89%]
ckanext/unaids/tests/test_validators.py EEEEEEEEEEEEEEEEE                [100%]

==================================== ERRORS ====================================
____ ERROR at setup of TestGetTableSchema.test_schema_returned_successfully ____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:10,516 INFO  [ckan.model] Database tables dropped
2026-01-10 07:49:12,330 INFO  [ckan.model] CKAN database version upgraded: base -> 4a5e3465beb6 (head)
2026-01-10 07:49:12,330 INFO  [ckan.model] Database initialised
2026-01-10 07:49:12,330 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:12,567 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:12,712 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:12,713 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:12,715 WARNI [ckan.common] Option lang is not declared
2026-01-10 07:49:12,767 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:244 Database tables dropped
INFO     ckan.model:__init__.py:376 CKAN database version upgraded: base -> 4a5e3465beb6 (head)
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.common:common.py:140 Option lang is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestGetTableSchema.test_empty_dict_returned_for_no_schema __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:12,841 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:12,842 INFO  [ckan.model] Database initialised
2026-01-10 07:49:12,842 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:12,918 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:13,002 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:13,003 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:13,058 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestPackageActivityList.test_package_activity_list_containes_releases_names _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:13,123 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:13,123 INFO  [ckan.model] Database initialised
2026-01-10 07:49:13,123 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:13,193 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:13,333 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:13,334 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:13,347 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:13,388 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestFormatGuess.test_format_guess_csv[art.csv-text/csv-CSV] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:13,457 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:13,458 INFO  [ckan.model] Database initialised
2026-01-10 07:49:13,458 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:13,540 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:13,670 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestFormatGuess.test_format_guess_csv[anc.xls-application/vnd.ms-excel-XLS] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:13,733 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:13,733 INFO  [ckan.model] Database initialised
2026-01-10 07:49:13,733 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:13,809 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:13,929 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestFormatGuess.test_format_guess_csv[country_regions.geojson-application/geo+json-GeoJSON] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:14,002 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:14,002 INFO  [ckan.model] Database initialised
2026-01-10 07:49:14,002 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:14,080 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:14,204 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestFormatGuess.test_format_guess_csv[spectrum_file.pjnz-application/pjnz-PJNZ] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:14,320 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:14,320 INFO  [ckan.model] Database initialised
2026-01-10 07:49:14,320 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:14,400 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:14,526 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestFormatGuess.test_format_guess_csv[no_file_extension-None-None] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:14,591 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:14,591 INFO  [ckan.model] Database initialised
2026-01-10 07:49:14,591 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:14,671 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:14,806 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_____ ERROR at setup of TestFormatGuess.test_format_guess_csv[-None-None] ______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:14,882 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:14,882 INFO  [ckan.model] Database initialised
2026-01-10 07:49:14,882 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:15,004 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:15,144 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________________ ERROR at setup of TestUserShowMe.test_no_user _________________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:15,204 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:15,204 INFO  [ckan.model] Database initialised
2026-01-10 07:49:15,204 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:15,284 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:15,416 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
__________________ ERROR at setup of TestUserShowMe.test_user __________________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:15,479 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:15,479 INFO  [ckan.model] Database initialised
2026-01-10 07:49:15,479 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:15,561 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:15,697 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestPopulateDataDictionary.test_expected_use ________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:15,766 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:15,766 INFO  [ckan.model] Database initialised
2026-01-10 07:49:15,766 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:15,888 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:15,969 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:15,970 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:16,022 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____ ERROR at setup of TestUserAffiliation.test_user_show_with_affiliation _____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:16,084 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:16,084 INFO  [ckan.model] Database initialised
2026-01-10 07:49:16,084 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:16,162 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:16,291 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestUserAffiliation.test_user_list_all_fields_with_affiliation _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:16,352 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:16,352 INFO  [ckan.model] Database initialised
2026-01-10 07:49:16,352 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:16,438 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:16,574 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestUserAffiliation.test_user_list_usernames ________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:16,629 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:16,629 INFO  [ckan.model] Database initialised
2026-01-10 07:49:16,629 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:16,753 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:16,885 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestPackageCreate.test_should_complain_with_exception_when_dataset_type_invalid _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:16,957 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:16,957 INFO  [ckan.model] Database initialised
2026-01-10 07:49:16,957 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:17,041 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:17,122 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:17,123 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:17,166 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestPackageCreate.test_create_dataset_without_type_creates_one_with_default_type_of_dataset _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:17,236 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:17,236 INFO  [ckan.model] Database initialised
2026-01-10 07:49:17,236 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:17,315 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:17,398 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:17,399 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:17,455 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestPackageCreate.test_create_dataset_with_valid_type ____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:17,512 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:17,512 INFO  [ckan.model] Database initialised
2026-01-10 07:49:17,512 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:17,645 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:17,732 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:17,733 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:17,783 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_displays_release[id] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:26,235 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:26,235 INFO  [ckan.model] Database initialised
2026-01-10 07:49:26,235 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:26,312 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:26,400 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:26,401 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:26,429 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:26,474 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_displays_release[name] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:26,541 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:26,542 INFO  [ckan.model] Database initialised
2026-01-10 07:49:26,542 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:26,625 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:26,711 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:26,712 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:26,730 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:26,771 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_raises_when_incorrect_release _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:26,840 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:26,840 INFO  [ckan.model] Database initialised
2026-01-10 07:49:26,840 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:26,982 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:27,073 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:27,074 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:27,094 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:27,138 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_returns_correct_download_links _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:27,204 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:27,205 INFO  [ckan.model] Database initialised
2026-01-10 07:49:27,205 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:27,290 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:27,374 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:27,375 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:27,398 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:27,436 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_only_returns_updated_resource_download_links_for_uploads _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:27,499 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:27,499 INFO  [ckan.model] Database initialised
2026-01-10 07:49:27,499 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:27,572 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:27,653 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:27,654 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:27,676 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:27,772 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_auth[admin-True] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:27,843 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:27,843 INFO  [ckan.model] Database initialised
2026-01-10 07:49:27,843 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:27,926 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:28,011 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:28,012 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:28,030 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:28,072 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_auth[editor-True] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:28,142 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:28,142 INFO  [ckan.model] Database initialised
2026-01-10 07:49:28,142 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:28,220 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:28,303 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:28,304 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:28,324 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:28,367 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetShowForRelease.test_package_show_auth[member-True] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:28,426 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:28,427 INFO  [ckan.model] Database initialised
2026-01-10 07:49:28,427 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:28,503 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:28,635 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:28,636 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:28,653 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:28,698 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_______ ERROR at setup of TestAuth.test_unaids_organization_update_valid _______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:28,761 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:28,761 INFO  [ckan.model] Database initialised
2026-01-10 07:49:28,761 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:28,839 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:28,924 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:28,928 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:28,929 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:28,971 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
______ ERROR at setup of TestAuth.test_unaids_organization_update_invalid ______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:29,033 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:29,033 INFO  [ckan.model] Database initialised
2026-01-10 07:49:29,033 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:29,118 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:29,206 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:29,210 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:29,211 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:29,256 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_________ ERROR at setup of TestAuth.test_dataset_lock[True-outcome0] __________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:29,370 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:29,370 INFO  [ckan.model] Database initialised
2026-01-10 07:49:29,370 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:29,451 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:29,536 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:29,540 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:29,542 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:29,583 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_________ ERROR at setup of TestAuth.test_dataset_lock[False-outcome1] _________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:29,646 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:29,646 INFO  [ckan.model] Database initialised
2026-01-10 07:49:29,646 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:29,728 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:29,829 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:29,833 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:29,834 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:29,875 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[False-False-False-outcome0] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:29,939 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:29,939 INFO  [ckan.model] Database initialised
2026-01-10 07:49:29,939 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:30,024 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:30,120 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:30,124 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:30,124 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:30,161 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[False-True-False-outcome1] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:30,273 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:30,273 INFO  [ckan.model] Database initialised
2026-01-10 07:49:30,273 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:30,356 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:30,459 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:30,463 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:30,464 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:30,510 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[False-False-True-outcome2] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:30,573 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:30,573 INFO  [ckan.model] Database initialised
2026-01-10 07:49:30,573 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:30,651 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:30,741 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:30,745 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:30,746 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:30,788 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[False-True-True-outcome3] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:30,862 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:30,862 INFO  [ckan.model] Database initialised
2026-01-10 07:49:30,862 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:30,940 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:31,082 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:31,085 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:31,086 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:31,130 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[True-False-False-outcome4] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:31,192 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:31,192 INFO  [ckan.model] Database initialised
2026-01-10 07:49:31,192 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:31,272 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:31,364 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:31,368 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:31,368 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:31,411 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[True-True-False-outcome5] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:31,477 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:31,477 INFO  [ckan.model] Database initialised
2026-01-10 07:49:31,477 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:31,560 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:31,658 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:31,662 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:31,662 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:31,714 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[True-False-True-outcome6] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:31,836 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:31,836 INFO  [ckan.model] Database initialised
2026-01-10 07:49:31,836 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:31,909 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:32,012 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:32,015 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:32,016 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:32,065 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAuth.test_locked_package_update[True-True-True-outcome7] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:32,133 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:32,133 INFO  [ckan.model] Database initialised
2026-01-10 07:49:32,133 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:32,213 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:32,305 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:32,310 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:32,311 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:32,350 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestVerifyRequiredScope.test_should_pass_when_scope_is_right _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:32,563 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:32,563 INFO  [ckan.model] Database initialised
2026-01-10 07:49:32,563 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:32,646 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:32,781 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestVerifyRequiredScope.test_should_complain_when_scope_is_none _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:32,892 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:32,892 INFO  [ckan.model] Database initialised
2026-01-10 07:49:32,892 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:32,974 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:33,102 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestVerifyRequiredScope.test_should_complain_when_scope_is_incorrect _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:33,169 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:33,169 INFO  [ckan.model] Database initialised
2026-01-10 07:49:33,169 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:33,252 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:33,376 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAccessTokenPresentAndValidAndUserAuthorized.test_should_set_user_when_token_correct_and_user_authorized _

make_app = <function _get_test_app at 0x76b7bdc6ef80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:33,436 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:33,436 INFO  [ckan.model] Database initialised
2026-01-10 07:49:33,436 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:33,441 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 07:49:33,511 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:33,692 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAccessTokenPresentAndValidAndUserAuthorized.test_should_return_false_when_no_token _

make_app = <function _get_test_app at 0x76b7bdc6ef80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:33,761 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:33,761 INFO  [ckan.model] Database initialised
2026-01-10 07:49:33,761 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:33,766 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 07:49:33,839 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:33,977 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestAccessTokenPresentAndValidAndUserAuthorized.test_should_throw_when_subject_of_different_origin_than_auth0 _

make_app = <function _get_test_app at 0x76b7bdc6ef80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:34,047 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:34,048 INFO  [ckan.model] Database initialised
2026-01-10 07:49:34,048 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:34,052 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 07:49:34,123 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:34,248 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidateAndDecodeToken.test_should_validate_and_decode_token _

make_app = <function _get_test_app at 0x76b7bdc6ef80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:34,462 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:34,462 INFO  [ckan.model] Database initialised
2026-01-10 07:49:34,462 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:34,467 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 07:49:34,537 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:34,670 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidateAndDecodeToken.test_should_throw_when_no_matching_rsa_key _

make_app = <function _get_test_app at 0x76b7bdc6ef80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:34,742 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:34,743 INFO  [ckan.model] Database initialised
2026-01-10 07:49:34,743 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:34,747 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 07:49:34,812 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:34,942 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidateAndDecodeToken.test_should_throw_when_token_expired _

make_app = <function _get_test_app at 0x76b7bdc6ef80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:35,068 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:35,068 INFO  [ckan.model] Database initialised
2026-01-10 07:49:35,068 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:35,074 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 07:49:35,159 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:35,289 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidateAndDecodeToken.test_should_throw_when_incorrect_claims _

make_app = <function _get_test_app at 0x76b7bdc6ef80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:35,361 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:35,362 INFO  [ckan.model] Database initialised
2026-01-10 07:49:35,362 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:35,366 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 07:49:35,432 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:35,563 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidateAndDecodeToken.test_should_throw_when_general_exception_thrown _

make_app = <function _get_test_app at 0x76b7bdc6ef80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:35,643 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:35,643 INFO  [ckan.model] Database initialised
2026-01-10 07:49:35,643 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:35,648 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 07:49:35,773 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:35,896 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestRegressionOAuth2PluginDoesntPreventVanillaCkanAuthentication.test_using_api_key _

make_app = <function _get_test_app at 0x76b7bdc6ef80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:35,971 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:35,971 INFO  [ckan.model] Database initialised
2026-01-10 07:49:35,971 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:35,976 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 07:49:36,051 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:36,134 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:36,135 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:36,181 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestRegressionOAuth2PluginDoesntPreventVanillaCkanAuthentication.test_using_api_token _

make_app = <function _get_test_app at 0x76b7bdc6ef80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:36,255 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:36,255 INFO  [ckan.model] Database initialised
2026-01-10 07:49:36,255 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:36,260 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 07:49:36,335 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:36,467 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:36,468 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:36,518 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidateUserProfileBlueprint.test_annonymous_access_to_index_page _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:36,595 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:36,596 INFO  [ckan.model] Database initialised
2026-01-10 07:49:36,596 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:36,673 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:36,820 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
______ ERROR at setup of TestMemberLists.test_org_member_download_200_ok _______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:36,884 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:36,885 INFO  [ckan.model] Database initialised
2026-01-10 07:49:36,885 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:36,960 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:37,095 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestMemberLists.test_org_member_download_expected_columns __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:37,158 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:37,158 INFO  [ckan.model] Database initialised
2026-01-10 07:49:37,158 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:37,235 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:37,425 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
__ ERROR at setup of TestMemberLists.test_org_member_download_expected_emails __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:37,489 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:37,489 INFO  [ckan.model] Database initialised
2026-01-10 07:49:37,489 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:37,568 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:37,701 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestMemberLists.test_org_member_download_403 ________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:37,765 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:37,765 INFO  [ckan.model] Database initialised
2026-01-10 07:49:37,765 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:37,838 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:38,023 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestMemberLists.test_org_member_download_404 ________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:38,086 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:38,086 INFO  [ckan.model] Database initialised
2026-01-10 07:49:38,086 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:38,165 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:38,311 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestMemberLists.test_org_member_download_anonymous_access __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:38,377 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:38,377 INFO  [ckan.model] Database initialised
2026-01-10 07:49:38,377 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:38,463 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:38,596 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseCreateAndEdit.test_create_with_valid_user _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:38,657 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:38,657 INFO  [ckan.model] Database initialised
2026-01-10 07:49:38,657 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:38,789 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:38,880 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:38,880 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:38,899 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:38,952 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseCreateAndEdit.test_create_with_invalid_user _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:39,025 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:39,025 INFO  [ckan.model] Database initialised
2026-01-10 07:49:39,025 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:39,113 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:39,200 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:39,201 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:39,221 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:39,268 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseCreateAndEdit.test_create_with_existing_activity_id _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:39,327 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:39,327 INFO  [ckan.model] Database initialised
2026-01-10 07:49:39,327 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:39,416 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:39,504 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:39,505 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:39,524 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:39,570 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseCreateAndEdit.test_create_with_existing_release_name _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:39,631 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:39,631 INFO  [ckan.model] Database initialised
2026-01-10 07:49:39,631 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:39,766 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:39,846 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:39,847 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:39,867 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:39,912 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseCreateAndEdit.test_edit_with_valid_user __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:39,973 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:39,973 INFO  [ckan.model] Database initialised
2026-01-10 07:49:39,973 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:40,051 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:40,134 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:40,135 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:40,158 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:40,207 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseCreateAndEdit.test_edit_with_invalid_user _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:40,272 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:40,272 INFO  [ckan.model] Database initialised
2026-01-10 07:49:40,272 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:40,353 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:40,442 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:40,443 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:40,463 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:40,508 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____ ERROR at setup of TestDatasetReleaseDelete.test_delete_with_valid_user ____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:40,576 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:40,576 INFO  [ckan.model] Database initialised
2026-01-10 07:49:40,576 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:40,716 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:40,803 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:40,804 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:40,824 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:40,868 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestDatasetReleaseDelete.test_delete_with_invalid_user ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:40,938 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:40,938 INFO  [ckan.model] Database initialised
2026-01-10 07:49:40,938 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:41,023 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:41,107 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:41,108 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:41,128 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:41,180 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestDatasetReleaseRestore.test_restore_with_valid_user ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:41,254 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:41,255 INFO  [ckan.model] Database initialised
2026-01-10 07:49:41,255 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:41,331 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:41,417 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:41,418 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:41,442 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:41,483 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
__ ERROR at setup of TestDatasetReleaseRestore.test_restore_with_invalid_user __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:41,549 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:41,550 INFO  [ckan.model] Database initialised
2026-01-10 07:49:41,550 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:41,681 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:41,768 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:41,769 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:41,792 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:41,834 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____ ERROR at setup of TestDatasetReleaseListView.test_no_releases_created _____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:41,901 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:41,902 INFO  [ckan.model] Database initialised
2026-01-10 07:49:41,902 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:41,980 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:42,069 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:42,070 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:42,092 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:42,136 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseListView.test_releases_are_listed_to_owners _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:42,193 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:42,193 INFO  [ckan.model] Database initialised
2026-01-10 07:49:42,193 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:42,274 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:42,355 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:42,356 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:42,378 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:42,421 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseListView.test_releases_are_listed_to_outsiders _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:42,482 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:42,483 INFO  [ckan.model] Database initialised
2026-01-10 07:49:42,483 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:42,624 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:42,716 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:42,716 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:42,739 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:42,782 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseListView.test_add_release_button_is_shown_to_owners _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:42,850 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:42,850 INFO  [ckan.model] Database initialised
2026-01-10 07:49:42,850 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:42,939 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:43,037 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:43,037 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:43,057 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:43,104 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetReleaseListView.test_add_release_button_is_not_shown_to_outsiders _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:43,165 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:43,165 INFO  [ckan.model] Database initialised
2026-01-10 07:49:43,165 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:43,242 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:43,322 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:43,323 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:43,343 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:43,388 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestDatasetRead.test_activity_id_query_param ________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:43,452 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:43,452 INFO  [ckan.model] Database initialised
2026-01-10 07:49:43,452 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:43,589 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:43,672 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:43,673 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:43,693 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:43,734 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
______ ERROR at setup of TestDatasetRead.test_when_no_release_for_dataset ______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:43,797 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:43,797 INFO  [ckan.model] Database initialised
2026-01-10 07:49:43,798 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:43,880 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:43,968 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:43,969 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:43,991 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:44,033 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetRead.test_when_on_latest_version_of_dataset_has_release _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:44,099 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:44,100 INFO  [ckan.model] Database initialised
2026-01-10 07:49:44,100 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:44,188 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:44,273 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:44,274 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:44,297 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:44,347 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_collaborator_transfering_dataset __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:44,407 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:44,407 INFO  [ckan.model] Database initialised
2026-01-10 07:49:44,407 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:44,547 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:44,650 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:44,654 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:44,655 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:44,694 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_get_org_admins_with_email_addresses_two_admins _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:44,753 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:44,753 INFO  [ckan.model] Database initialised
2026-01-10 07:49:44,753 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:44,832 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:44,940 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:44,944 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:44,945 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:44,987 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_get_org_admins_with_email_addresses_one_admin _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:45,051 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:45,051 INFO  [ckan.model] Database initialised
2026-01-10 07:49:45,051 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:45,130 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:45,243 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:45,248 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:45,250 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:45,290 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_get_org_admins_with_email_addresses_no_admins _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:45,348 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:45,349 INFO  [ckan.model] Database initialised
2026-01-10 07:49:45,349 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:45,484 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:45,593 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:45,596 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:45,597 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:45,637 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_get_org_admins_with_email_addresses_two_orgs _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:45,702 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:45,702 INFO  [ckan.model] Database initialised
2026-01-10 07:49:45,702 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:45,790 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:45,894 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:45,897 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:45,898 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:45,941 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_get_org_admins_with_no_users_in_org _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:46,005 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:46,006 INFO  [ckan.model] Database initialised
2026-01-10 07:49:46,006 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:46,083 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:46,189 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:46,193 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:46,193 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:46,235 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestDatasetTransfer.test_send_dataset_transfer_emails ____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:46,294 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:46,294 INFO  [ckan.model] Database initialised
2026-01-10 07:49:46,294 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:46,425 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:46,520 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:46,524 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:46,525 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:46,564 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_send_dataset_transfer_email_subject _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:46,622 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:46,622 INFO  [ckan.model] Database initialised
2026-01-10 07:49:46,622 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:46,701 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:46,810 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:46,814 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:46,815 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:46,849 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestDatasetTransfer.test_send_dataset_transfer_emails_errors _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:46,911 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:46,912 INFO  [ckan.model] Database initialised
2026-01-10 07:49:46,912 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:46,989 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:47,093 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:47,097 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:47,097 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:47,138 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_____ ERROR at setup of TestDatasetTransfer.test_dataset_transfer_request ______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:47,200 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:47,200 INFO  [ckan.model] Database initialised
2026-01-10 07:49:47,200 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:47,334 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:47,433 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:47,436 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:47,437 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:47,480 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
__ ERROR at setup of TestResourceUrlEncoding.test_resource_url_encoding_test ___

make_app = <function _get_test_app at 0x76b7bdc6ef80>

    @pytest.fixture
    def app(make_app):
        """Returns a client app instance to use in functional tests
    
        To use it, just add the ``app`` parameter to your test function signature::
    
            def test_dataset_search(self, app):
    
                url = h.url_for('dataset.search')
    
                response = app.get(url)
    
    
        """
>       return make_app()

../ckan/ckan/tests/pytest_ckan/fixtures.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:275: in _get_test_app
    app = ckan.config.middleware.make_app(config)
../ckan/ckan/config/middleware/__init__.py:29: in make_app
    load_environment(conf)
../ckan/ckan/config/environment.py:68: in load_environment
    p.load_all()
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:47,542 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:47,542 INFO  [ckan.model] Database initialised
2026-01-10 07:49:47,542 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:47,546 INFO  [ckan.config.environment] Loading static files from public
2026-01-10 07:49:47,626 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:47,714 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:47,715 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:47,775 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:53 Loading static files from public
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestDatasetLockHelper.test_dataset_lockable _________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:47,844 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:47,845 INFO  [ckan.model] Database initialised
2026-01-10 07:49:47,845 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:47,918 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:48,016 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:48,019 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:48,020 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:48,056 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
______ ERROR at setup of TestDatasetLockHelper.test_dataset_not_lockable _______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:48,115 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:48,115 INFO  [ckan.model] Database initialised
2026-01-10 07:49:48,115 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:48,193 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:48,347 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:48,351 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:48,351 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:48,392 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of TestDatasetLockHelper.test_dataset_not_found ________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:48,460 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:48,460 INFO  [ckan.model] Database initialised
2026-01-10 07:49:48,460 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:48,533 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:48,624 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:48,628 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:48,629 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:48,667 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[validates if all upload fields correct] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:48,733 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:48,733 INFO  [ckan.model] Database initialised
2026-01-10 07:49:48,733 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:48,815 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:48,944 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[validates if no upload fields present] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:49,015 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:49,015 INFO  [ckan.model] Database initialised
2026-01-10 07:49:49,015 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:49,093 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:49,226 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[validates if all fields are present but empty] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:49,344 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:49,344 INFO  [ckan.model] Database initialised
2026-01-10 07:49:49,344 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:49,431 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:49,569 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if some fields missing and others set to empty string] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:49,628 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:49,628 INFO  [ckan.model] Database initialised
2026-01-10 07:49:49,628 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:49,711 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:49,844 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
__ ERROR at setup of test_validate_resource_upload_fields[fails if size None] __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:49,907 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:49,908 INFO  [ckan.model] Database initialised
2026-01-10 07:49:49,908 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:49,987 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:50,124 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if only size present] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:50,190 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:50,190 INFO  [ckan.model] Database initialised
2026-01-10 07:49:50,190 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:50,319 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:50,454 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if sha256 empty string] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:50,519 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:50,519 INFO  [ckan.model] Database initialised
2026-01-10 07:49:50,519 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:50,597 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:50,737 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if sha256 None] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:50,799 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:50,799 INFO  [ckan.model] Database initialised
2026-01-10 07:49:50,799 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:50,873 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:51,007 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if only sha256 present] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:51,067 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:51,068 INFO  [ckan.model] Database initialised
2026-01-10 07:49:51,068 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:51,148 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:51,287 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if invalid sha256] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:51,401 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:51,401 INFO  [ckan.model] Database initialised
2026-01-10 07:49:51,401 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:51,475 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:51,608 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if lfs_prefix empty string] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:51,670 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:51,671 INFO  [ckan.model] Database initialised
2026-01-10 07:49:51,671 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:51,754 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:51,893 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if lfs_prefix None] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:51,958 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:51,959 INFO  [ckan.model] Database initialised
2026-01-10 07:49:51,959 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:52,040 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:52,247 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of test_validate_resource_upload_fields[fails if only lfs_prefix present] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:52,316 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:52,316 INFO  [ckan.model] Database initialised
2026-01-10 07:49:52,316 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:52,404 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:52,543 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________ ERROR at setup of test_update_filename_in_upload_resource_url _________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:52,603 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:52,603 INFO  [ckan.model] Database initialised
2026-01-10 07:49:52,603 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:52,680 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:52,766 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:52,767 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:52,816 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____ ERROR at setup of test_update_filename_in_link_resource_url[http url] _____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:52,876 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:52,876 INFO  [ckan.model] Database initialised
2026-01-10 07:49:52,876 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:52,962 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:53,052 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:53,052 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:53,108 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____ ERROR at setup of test_update_filename_in_link_resource_url[https url] ____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:53,221 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:53,222 INFO  [ckan.model] Database initialised
2026-01-10 07:49:53,222 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:53,302 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:53,383 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:53,384 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:53,435 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
__ ERROR at setup of test_update_filename_in_link_resource_url[url with path] __

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:53,498 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:53,498 INFO  [ckan.model] Database initialised
2026-01-10 07:49:53,498 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:53,576 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:53,664 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:53,665 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:53,717 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
______ ERROR at setup of TestPlugin.test_geojson_format_guessed_correctly ______

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:54,141 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:54,141 INFO  [ckan.model] Database initialised
2026-01-10 07:49:54,141 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:54,213 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:54,293 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:54,294 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:54,346 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestPlugin.test_blob_storage_validator_is_used_during_resource_actions _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:54,421 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:54,421 INFO  [ckan.model] Database initialised
2026-01-10 07:49:54,421 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:54,496 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:54,639 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:54,640 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:54,699 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_________ ERROR at setup of TestValidatePackage.test_validate_package __________

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:164: in load
    service = _get_service(plugin)
../ckan/ckan/plugins/core.py:284: in _get_service
    return plugin.load()(name=plugin_name)
/usr/local/lib/python3.10/site-packages/pkg_resources/__init__.py:2777: in load
    return self.resolve()
/usr/local/lib/python3.10/site-packages/pkg_resources/__init__.py:2783: in resolve
    module = __import__(self.module_name, fromlist=['__name__'], level=0)
../ckanext-validation/ckanext/validation/plugin/__init__.py:11: in <module>
    from ckanext.validation.logic import (
../ckanext-validation/ckanext/validation/logic.py:16: in <module>
    from ckanext.validation.jobs import run_validation_job
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

    # encoding: utf-8
    
    import logging
    import datetime
    import json
    import re
    
    import requests
    from sqlalchemy.orm.exc import NoResultFound
>   from frictionless import system, Resource, Package, Report, Schema, Dialect, Check, Checklist, Detector
E   ModuleNotFoundError: No module named 'frictionless'

../ckanext-validation/ckanext/validation/jobs.py:10: ModuleNotFoundError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:54,760 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:54,760 INFO  [ckan.model] Database initialised
2026-01-10 07:49:54,760 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:54,840 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidatePackage.test_metadata_change_does_not_validate_package _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:164: in load
    service = _get_service(plugin)
../ckan/ckan/plugins/core.py:284: in _get_service
    return plugin.load()(name=plugin_name)
/usr/local/lib/python3.10/site-packages/pkg_resources/__init__.py:2777: in load
    return self.resolve()
/usr/local/lib/python3.10/site-packages/pkg_resources/__init__.py:2783: in resolve
    module = __import__(self.module_name, fromlist=['__name__'], level=0)
../ckanext-validation/ckanext/validation/plugin/__init__.py:11: in <module>
    from ckanext.validation.logic import (
../ckanext-validation/ckanext/validation/logic.py:16: in <module>
    from ckanext.validation.jobs import run_validation_job
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

    # encoding: utf-8
    
    import logging
    import datetime
    import json
    import re
    
    import requests
    from sqlalchemy.orm.exc import NoResultFound
>   from frictionless import system, Resource, Package, Report, Schema, Dialect, Check, Checklist, Detector
E   ModuleNotFoundError: No module named 'frictionless'

../ckanext-validation/ckanext/validation/jobs.py:10: ModuleNotFoundError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:55,024 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:55,024 INFO  [ckan.model] Database initialised
2026-01-10 07:49:55,024 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:55,110 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____ ERROR at setup of TestValidators.test_organisation_id_exists_validator ____

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:55,493 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:55,493 INFO  [ckan.model] Database initialised
2026-01-10 07:49:55,493 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:55,572 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:55,654 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:55,655 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:55,702 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_if_empty_guess_format[None-None-http://adr.local/resource/8919/demo.csv-text/csv] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:55,764 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:55,764 INFO  [ckan.model] Database initialised
2026-01-10 07:49:55,764 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:55,846 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:55,987 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:55,987 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:56,044 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_if_empty_guess_format[None-None-http://adr.local/resource/8919/demo.geojson-application/geo+json] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:56,108 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:56,109 INFO  [ckan.model] Database initialised
2026-01-10 07:49:56,109 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:56,191 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:56,270 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:56,271 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:56,317 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_if_empty_guess_format[None-None-http://adr.local/resource/8919/demo.pjnz-application/pjnz] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:56,373 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:56,373 INFO  [ckan.model] Database initialised
2026-01-10 07:49:56,373 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:56,452 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:56,537 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:56,538 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:56,587 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_if_empty_guess_format[text/csv-8919-http://adr.local/resource/89192/demo.xlsx-text/csv] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:56,659 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:56,659 INFO  [ckan.model] Database initialised
2026-01-10 07:49:56,659 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:56,831 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:56,926 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:56,927 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:56,978 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_if_empty_guess_format[None-8919-http://adr.local/resource/8919/demo.csv-None] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:57,044 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:57,045 INFO  [ckan.model] Database initialised
2026-01-10 07:49:57,045 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:57,128 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:57,221 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:57,222 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:57,280 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context0-result0] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:57,342 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:57,343 INFO  [ckan.model] Database initialised
2026-01-10 07:49:57,343 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:57,422 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:57,504 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:57,505 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:57,553 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context1-result1] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:57,620 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:57,621 INFO  [ckan.model] Database initialised
2026-01-10 07:49:57,621 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:57,754 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:57,841 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:57,842 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:57,891 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context2-result2] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:57,954 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:57,954 INFO  [ckan.model] Database initialised
2026-01-10 07:49:57,954 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:58,035 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:58,120 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:58,121 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:58,168 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context3-result3] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:58,237 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:58,238 INFO  [ckan.model] Database initialised
2026-01-10 07:49:58,238 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:58,316 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:58,395 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:58,396 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:58,445 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context4-result4] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:58,514 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:58,514 INFO  [ckan.model] Database initialised
2026-01-10 07:49:58,514 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:58,663 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:58,772 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:58,773 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:58,821 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context5-result5] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:58,883 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:58,883 INFO  [ckan.model] Database initialised
2026-01-10 07:49:58,883 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:58,971 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:59,061 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:59,062 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:59,107 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context6-result6] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:59,170 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:59,170 INFO  [ckan.model] Database initialised
2026-01-10 07:49:59,170 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:59,250 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:59,336 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:59,337 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:59,384 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context7-result7] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:59,450 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:59,451 INFO  [ckan.model] Database initialised
2026-01-10 07:49:59,451 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:59,594 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:59,683 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:59,683 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:59,726 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
___ ERROR at setup of TestValidators.test_read_only_raises[context8-result8] ___

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:59,793 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:59,793 INFO  [ckan.model] Database initialised
2026-01-10 07:49:59,793 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:59,871 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:59,949 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:59,950 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:59,995 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_read_only_sets_default_value[context0-data0-false] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:50:00,063 INFO  [ckan.model] Database table data deleted
2026-01-10 07:50:00,063 INFO  [ckan.model] Database initialised
2026-01-10 07:50:00,064 INFO  [ckan.model] Database rebuilt
2026-01-10 07:50:00,135 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:50:00,220 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:50:00,221 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:50:00,269 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_ ERROR at setup of TestValidators.test_read_only_sets_default_value[context1-data1-false] _

ckan_config = {'use': 'config:/srv/app/src/ckan/test-core.ini', 'debug': False, 'testing': True, 'SECRET_KEY': 'This_is_a_secret_or_...MAX_COOKIE_SIZE': 4093, 'BABEL_TRANSLATION_DIRECTORIES': '/srv/app/src/ckan/ckan/i18n', 'PREFERRED_URL_SCHEME': 'http'}

    @pytest.fixture
    def with_plugins(ckan_config):
        """Load all plugins specified by the ``ckan.plugins`` config option at the
        beginning of the test(and disable any plugin which is not listed inside
        ``ckan.plugins``). When the test ends (including fail), it will unload all
        the plugins.
    
        .. literalinclude:: /../ckan/tests/test_factories.py
           :start-after: # START-CONFIG-OVERRIDE
           :end-before: # END-CONFIG-OVERRIDE
    
        Use this fixture if test relies on CKAN plugin infrastructure. For example,
        if test calls an action or helper registered by plugin XXX::
    
            @pytest.mark.ckan_config("ckan.plugins", "XXX")
            @pytest.mark.usefixtures("with_plugin")
            def test_action_and_helper():
                assert call_action("xxx_action")
                assert tk.h.xxx_helper()
    
        It will not work without ``with_plugins``. If ``XXX`` plugin is not loaded,
        ``xxx_action`` and ``xxx_helper`` do not exist in CKAN registries.
    
        But if the test above use direct imports instead, ``with_plugins`` is
        optional::
    
            def test_action_and_helper():
                from ckanext.xxx.logic.action import xxx_action
                from ckanext.xxx.helpers import xxx_helper
    
                assert xxx_action()
                assert xxx_helper()
    
        Keep in mind, that generally it's a bad idea to import helpers and actions
        directly. If **every** test of extension requires standard set of plugins,
        specify these plugins inside test config file(``test.ini``)::
    
            ckan.plugins = essential_plugin another_plugin_required_by_every_test
    
        And create an autouse-fixture that depends on ``with_plugins`` inside
        the main ``conftest.py`` (``ckanext/ext/tests/conftest.py``)::
    
            @pytest.fixture(autouse=True)
            def load_standard_plugins(with_plugins):
                ...
    
        This will automatically enable ``with_plugins`` for every test, even if
        it's not required explicitely.
    
        """
>       ckan.plugins.load_all()

../ckan/ckan/tests/pytest_ckan/fixtures.py:427: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/plugins/core.py:148: in load_all
    load(*plugins)
../ckan/ckan/plugins/core.py:180: in load
    plugins_update()
../ckan/ckan/plugins/core.py:136: in plugins_update
    environment.update_config()
../ckan/ckan/config/environment.py:237: in update_config
    logic.get_action('get_site_user')({'ignore_auth': True}, {})
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

action = 'get_site_user'

    def get_action(action: str) -> Action:
        '''Return the named :py:mod:`ckan.logic.action` function.
    
        For example ``get_action('package_create')`` will normally return the
        :py:func:`ckan.logic.action.create.package_create()` function.
    
        For documentation of the available action functions, see
        :ref:`api-reference`.
    
        You should always use ``get_action()`` instead of importing an action
        function directly, because :py:class:`~ckan.plugins.interfaces.IActions`
        plugins can override action functions, causing ``get_action()`` to return a
        plugin-provided function instead of the default one.
    
        Usage::
    
            import ckan.plugins.toolkit as toolkit
    
            # Call the package_create action function:
            toolkit.get_action('package_create')(context, data_dict)
    
        As the context parameter passed to an action function is commonly::
    
            context = {'model': ckan.model, 'session': ckan.model.Session,
                       'user': user}
    
        an action function returned by ``get_action()`` will automatically add
        these parameters to the context if they are not defined.  This is
        especially useful for plugins as they should not really be importing parts
        of ckan eg :py:mod:`ckan.model` and as such do not have access to ``model``
        or ``model.Session``.
    
        If a ``context`` of ``None`` is passed to the action function then the
        default context dict will be created.
    
        .. note::
    
            Many action functions modify the context dict. It can therefore
            not be reused for multiple calls of the same or different action
            functions.
    
        :param action: name of the action function to return,
            eg. ``'package_create'``
        :type action: string
    
        :returns: the named action function
        :rtype: callable
    
        '''
    
        if _actions:
            if action not in _actions:
                raise KeyError("Action '%s' not found" % action)
            return _actions[action]
        # Otherwise look in all the plugins to resolve all possible First
        # get the default ones in the ckan/logic/action directory Rather
        # than writing them out in full will use importlib.import_module
        # to load anything from ckan.logic.action that looks like it might
        # be an action
        for action_module_name in ['get', 'create', 'update', 'delete', 'patch']:
            module = importlib.import_module(
                '.' + action_module_name, 'ckan.logic.action')
            for k, v in authz.get_local_functions(module):
                _actions[k] = v
                # Allow all actions defined in logic/action/get.py to
                # be side-effect free.
                if action_module_name == 'get' and \
                   not hasattr(v, 'side_effect_free'):
                    v.side_effect_free = True
    
        # Then overwrite them with any specific ones in the plugins:
        resolved_action_plugins: dict[str, str] = {}
        fetched_actions = {}
        chained_actions = defaultdict(list)
        for plugin in p.PluginImplementations(p.IActions):
            for name, action_function in plugin.get_actions().items():
                if _is_chained_action(action_function):
                    chained_actions[name].append(action_function)
                elif name in resolved_action_plugins:
                    raise NameConflict(
                        'The action %r is already implemented in %r' % (
                            name,
                            resolved_action_plugins[name]
                        )
                    )
                else:
                    resolved_action_plugins[name] = plugin.name
                    # Extensions are exempted from the auth audit for now
                    # This needs to be resolved later
                    # type_ignore_reason: custom attribute
                    action_function.auth_audit_exempt = True  # type: ignore
                    fetched_actions[name] = action_function
        for name, func_list in chained_actions.items():
            if name not in fetched_actions and name not in _actions:
                # nothing to override from plugins or core
>               raise NotFound('The action %r is not found for chained action' % (
                    name))
E               ckan.logic.NotFound: The action 'package_activity_list' is not found for chained action

../ckan/ckan/logic/__init__.py:541: NotFound
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:50:00,329 INFO  [ckan.model] Database table data deleted
2026-01-10 07:50:00,329 INFO  [ckan.model] Database initialised
2026-01-10 07:50:00,329 INFO  [ckan.model] Database rebuilt
2026-01-10 07:50:00,471 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:50:00,558 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:50:00,559 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:50:00,613 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
=================================== FAILURES ===================================
___________________ TestDatasetUnlock.test_metadata_updated ____________________

self = <ckanext.unaids.tests.test_actions_dataset_lock.TestDatasetUnlock object at 0x76b7bafd3c10>
locked_dataset = {'author': None, 'author_email': None, 'creator_user_id': '7d6ad754-c1b0-44df-9468-d5632d8f8094', 'groups': [], ...}

    def test_metadata_updated(self, locked_dataset):
>       call_action(
            'dataset_unlock',
            id=locked_dataset['id']
        )

ckanext/unaids/tests/test_actions_dataset_lock.py:66: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:128: in call_action
    return logic.get_action(action_name)(context, kwargs)
../ckan/ckan/logic/__init__.py:580: in wrapped
    result = _action(context, data_dict, **kw)
ckanext/unaids/actions.py:319: in dataset_unlock
    t.get_action("package_patch")(
../ckan/ckan/logic/__init__.py:580: in wrapped
    result = _action(context, data_dict, **kw)
../ckan/ckan/logic/action/patch.py:53: in package_patch
    return _get_action('package_update')(context, patched)
../ckan/ckan/logic/__init__.py:596: in wrapped
    signals.action_succeeded.send(
/usr/local/lib/python3.10/site-packages/blinker/base.py:279: in send
    result = receiver(sender, **kwargs)
../ckan/ckanext/activity/subscriptions.py:167: in package_changed
    _create_package_activity(
../ckan/ckanext/activity/subscriptions.py:82: in _create_package_activity
    user_obj = _get_user_or_raise(context["user"])
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

username = '127.0.0.1'

    def _get_user_or_raise(username: str) -> model.User:
        """Get user by username or raise standard exception.
        """
        if user := model.User.get(username):
            return user
    
>       raise tk.ValidationError({
            "user_id": ["User not found"]
        })
E       ckan.logic.ValidationError: None - {'user_id': ['User not found']}

../ckan/ckanext/activity/subscriptions.py:145: ValidationError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:21,356 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:21,356 INFO  [ckan.model] Database initialised
2026-01-10 07:49:21,356 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:21,436 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:21,529 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:21,537 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:21,538 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:21,578 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
--------------------------- Captured stdout teardown ---------------------------
2026-01-10 07:49:22,810 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
---------------------------- Captured log teardown -----------------------------
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
____________________ TestDatasetUnlock.test_version_deleted ____________________

self = <ckanext.unaids.tests.test_actions_dataset_lock.TestDatasetUnlock object at 0x76b7bafd3e50>
locked_dataset = {'author': None, 'author_email': None, 'creator_user_id': 'b1e35fef-6280-432b-942e-beb0d5818411', 'groups': [], ...}

    def test_version_deleted(self, locked_dataset):
>       call_action(
            'dataset_unlock',
            id=locked_dataset['id']
        )

ckanext/unaids/tests/test_actions_dataset_lock.py:77: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:128: in call_action
    return logic.get_action(action_name)(context, kwargs)
../ckan/ckan/logic/__init__.py:580: in wrapped
    result = _action(context, data_dict, **kw)
ckanext/unaids/actions.py:319: in dataset_unlock
    t.get_action("package_patch")(
../ckan/ckan/logic/__init__.py:580: in wrapped
    result = _action(context, data_dict, **kw)
../ckan/ckan/logic/action/patch.py:53: in package_patch
    return _get_action('package_update')(context, patched)
../ckan/ckan/logic/__init__.py:596: in wrapped
    signals.action_succeeded.send(
/usr/local/lib/python3.10/site-packages/blinker/base.py:279: in send
    result = receiver(sender, **kwargs)
../ckan/ckanext/activity/subscriptions.py:167: in package_changed
    _create_package_activity(
../ckan/ckanext/activity/subscriptions.py:82: in _create_package_activity
    user_obj = _get_user_or_raise(context["user"])
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

username = '127.0.0.1'

    def _get_user_or_raise(username: str) -> model.User:
        """Get user by username or raise standard exception.
        """
        if user := model.User.get(username):
            return user
    
>       raise tk.ValidationError({
            "user_id": ["User not found"]
        })
E       ckan.logic.ValidationError: None - {'user_id': ['User not found']}

../ckan/ckanext/activity/subscriptions.py:145: ValidationError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:22,859 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:22,859 INFO  [ckan.model] Database initialised
2026-01-10 07:49:22,859 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:22,937 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:23,080 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:23,088 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:23,089 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:23,131 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
--------------------------- Captured stdout teardown ---------------------------
2026-01-10 07:49:24,453 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
---------------------------- Captured log teardown -----------------------------
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
________________ TestDatasetUnlock.test_version_already_deleted ________________

self = <ckanext.unaids.tests.test_actions_dataset_lock.TestDatasetUnlock object at 0x76b7bb0000d0>
locked_dataset = {'author': None, 'author_email': None, 'creator_user_id': 'eb288bf7-8615-4bc8-a4a2-d19f60eda5bf', 'groups': [], ...}

    def test_version_already_deleted(self, locked_dataset):
        versions = call_action('dataset_version_list', dataset_id=locked_dataset['id'])
        call_action(
            "version_delete",
            version_id=versions[-1]["id"],
        )
        with pytest.raises(toolkit.ObjectNotFound):
>           call_action(
                'dataset_unlock',
                id=locked_dataset['id']
            )

ckanext/unaids/tests/test_actions_dataset_lock.py:91: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../ckan/ckan/tests/helpers.py:128: in call_action
    return logic.get_action(action_name)(context, kwargs)
../ckan/ckan/logic/__init__.py:580: in wrapped
    result = _action(context, data_dict, **kw)
ckanext/unaids/actions.py:319: in dataset_unlock
    t.get_action("package_patch")(
../ckan/ckan/logic/__init__.py:580: in wrapped
    result = _action(context, data_dict, **kw)
../ckan/ckan/logic/action/patch.py:53: in package_patch
    return _get_action('package_update')(context, patched)
../ckan/ckan/logic/__init__.py:596: in wrapped
    signals.action_succeeded.send(
/usr/local/lib/python3.10/site-packages/blinker/base.py:279: in send
    result = receiver(sender, **kwargs)
../ckan/ckanext/activity/subscriptions.py:167: in package_changed
    _create_package_activity(
../ckan/ckanext/activity/subscriptions.py:82: in _create_package_activity
    user_obj = _get_user_or_raise(context["user"])
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

username = '127.0.0.1'

    def _get_user_or_raise(username: str) -> model.User:
        """Get user by username or raise standard exception.
        """
        if user := model.User.get(username):
            return user
    
>       raise tk.ValidationError({
            "user_id": ["User not found"]
        })
E       ckan.logic.ValidationError: None - {'user_id': ['User not found']}

../ckan/ckanext/activity/subscriptions.py:145: ValidationError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:24,498 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:24,499 INFO  [ckan.model] Database initialised
2026-01-10 07:49:24,499 INFO  [ckan.model] Database rebuilt
2026-01-10 07:49:24,575 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
2026-01-10 07:49:24,665 WARNI [ckan.lib.webassets_tools] Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
2026-01-10 07:49:24,672 WARNI [ckan.common] Option scheming.dataset_schemas is not declared
2026-01-10 07:49:24,673 WARNI [ckan.common] Option scheming.dataset_schemas_directory is not declared
2026-01-10 07:49:24,715 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
WARNING  ckan.lib.webassets_tools:webassets_tools.py:36 Cannot create library versions at /srv/app/src/ckanext-versions/ckanext/versions/fanstatic because webassets.yaml is missing
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas is not declared
WARNING  ckan.common:common.py:140 Option scheming.dataset_schemas_directory is not declared
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
--------------------------- Captured stdout teardown ---------------------------
2026-01-10 07:49:26,185 INFO  [ckan.config.environment] Loading templates from /srv/app/src/ckan/ckan/templates
---------------------------- Captured log teardown -----------------------------
INFO     ckan.config.environment:environment.py:202 Loading templates from /srv/app/src/ckan/ckan/templates
_______________ TestAutoPopulateDataDictionaries.test_no_schema ________________

self = <ckanext.unaids.tests.test_logic.TestAutoPopulateDataDictionaries object at 0x76b78fc65c00>
mocker = <pytest_mock.plugin.MockerFixture object at 0x76b78e7fd6f0>

    def test_no_schema(self, mocker):
        context = {}
>       dataset = factories.Dataset()

ckanext/unaids/tests/test_logic.py:86: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/usr/local/lib/python3.10/site-packages/factory/base.py:40: in __call__
    return cls.create(**kwargs)
/usr/local/lib/python3.10/site-packages/factory/base.py:528: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
/usr/local/lib/python3.10/site-packages/factory/alchemy.py:60: in _generate
    return super()._generate(strategy, params)
/usr/local/lib/python3.10/site-packages/factory/base.py:465: in _generate
    return step.build()
/usr/local/lib/python3.10/site-packages/factory/builder.py:274: in build
    instance = self.factory_meta.instantiate(
/usr/local/lib/python3.10/site-packages/factory/base.py:317: in instantiate
    return self.factory._create(model, *args, **kwargs)
../ckan/ckan/tests/factories.py:220: in _create
    return cls.api_create(kwargs)
../ckan/ckan/tests/factories.py:203: in api_create
    result = helpers.call_action(cls._meta.action, **data_dict)
../ckan/ckan/tests/helpers.py:128: in call_action
    return logic.get_action(action_name)(context, kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

context = {'ignore_auth': True, 'user': 'test.ckan.net'}
data_dict = {'name': 'dataset-whou-7444-vsod', 'notes': 'Sister picture kind within television.', 'title': 'Even include move arrive move answer probably.'}

    def package_create(
            context: Context, data_dict: DataDict) -> ActionResult.PackageCreate:
        '''Create a new dataset (package).
    
        You must be authorized to create new datasets. If you specify any groups
        for the new dataset, you must also be authorized to edit these groups.
    
        Plugins may change the parameters of this function depending on the value
        of the ``type`` parameter, see the
        :py:class:`~ckan.plugins.interfaces.IDatasetForm` plugin interface.
    
        :param name: the name of the new dataset, must be between 2 and 100
            characters long and contain only lowercase alphanumeric characters,
            ``-`` and ``_``, e.g. ``'warandpeace'``
        :type name: string
        :param title: the title of the dataset (optional, default: same as
            ``name``)
        :type title: string
        :param private: If ``True`` creates a private dataset
        :type private: bool
        :param author: the name of the dataset's author (optional)
        :type author: string
        :param author_email: the email address of the dataset's author (optional)
        :type author_email: string
        :param maintainer: the name of the dataset's maintainer (optional)
        :type maintainer: string
        :param maintainer_email: the email address of the dataset's maintainer
            (optional)
        :type maintainer_email: string
        :param license_id: the id of the dataset's license, see
            :py:func:`~ckan.logic.action.get.license_list` for available values
            (optional)
        :type license_id: license id string
        :param notes: a description of the dataset (optional)
        :type notes: string
        :param url: a URL for the dataset's source (optional)
        :type url: string
        :param version: (optional)
        :type version: string, no longer than 100 characters
        :param state: the current state of the dataset, e.g. ``'active'`` or
            ``'deleted'``, only active datasets show up in search results and
            other lists of datasets, this parameter will be ignored if you are not
            authorized to change the state of the dataset (optional, default:
            ``'active'``)
        :type state: string
        :param type: the type of the dataset (optional),
            :py:class:`~ckan.plugins.interfaces.IDatasetForm` plugins
            associate themselves with different dataset types and provide custom
            dataset handling behaviour for these types
        :type type: string
        :param resources: the dataset's resources, see
            :py:func:`resource_create` for the format of resource dictionaries
            (optional)
        :type resources: list of resource dictionaries
        :param tags: the dataset's tags, see :py:func:`tag_create` for the format
            of tag dictionaries (optional)
        :type tags: list of tag dictionaries
        :param extras: the dataset's extras (optional), extras are arbitrary
            (key: value) metadata items that can be added to datasets, each extra
            dictionary should have keys ``'key'`` (a string), ``'value'`` (a
            string)
        :type extras: list of dataset extra dictionaries
        :param plugin_data: private package data belonging to plugins.
            Only sysadmin users may set this value. It should be a dict that can
            be dumped into JSON, and plugins should namespace their data with the
            plugin name to avoid collisions with other plugins, eg::
    
                {
                    "name": "test-dataset",
                    "plugin_data": {
                        "plugin1": {"key1": "value1"},
                        "plugin2": {"key2": "value2"}
                    }
                }
        :type plugin_data: dict
        :param relationships_as_object: see :py:func:`package_relationship_create`
            for the format of relationship dictionaries (optional)
        :type relationships_as_object: list of relationship dictionaries
        :param relationships_as_subject: see :py:func:`package_relationship_create`
            for the format of relationship dictionaries (optional)
        :type relationships_as_subject: list of relationship dictionaries
        :param groups: the groups to which the dataset belongs (optional), each
            group dictionary should have one or more of the following keys which
            identify an existing group:
            ``'id'`` (the id of the group, string), or ``'name'`` (the name of the
            group, string),  to see which groups exist
            call :py:func:`~ckan.logic.action.get.group_list`
        :type groups: list of dictionaries
        :param owner_org: the id of the dataset's owning organization, see
            :py:func:`~ckan.logic.action.get.organization_list` or
            :py:func:`~ckan.logic.action.get.organization_list_for_user` for
            available values. This parameter can be made optional if the config
            option :ref:`ckan.auth.create_unowned_dataset` is set to ``True``.
        :type owner_org: string
    
        :returns: the newly created dataset (unless 'return_id_only' is set to True
                  in the context, in which case just the dataset id will
                  be returned)
        :rtype: dictionary
    
        '''
>       model = context['model']
E       KeyError: 'model'

../ckan/ckan/logic/action/create.py:156: KeyError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:53,780 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:53,781 INFO  [ckan.model] Database initialised
2026-01-10 07:49:53,781 INFO  [ckan.model] Database rebuilt
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
_____________ TestAutoPopulateDataDictionaries.test_missing_schema _____________

self = <ckanext.unaids.tests.test_logic.TestAutoPopulateDataDictionaries object at 0x76b78fc65e40>
mocker = <pytest_mock.plugin.MockerFixture object at 0x76b78eb89090>

    def test_missing_schema(self, mocker):
        context = {}
>       dataset = factories.Dataset()

ckanext/unaids/tests/test_logic.py:98: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/usr/local/lib/python3.10/site-packages/factory/base.py:40: in __call__
    return cls.create(**kwargs)
/usr/local/lib/python3.10/site-packages/factory/base.py:528: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
/usr/local/lib/python3.10/site-packages/factory/alchemy.py:60: in _generate
    return super()._generate(strategy, params)
/usr/local/lib/python3.10/site-packages/factory/base.py:465: in _generate
    return step.build()
/usr/local/lib/python3.10/site-packages/factory/builder.py:274: in build
    instance = self.factory_meta.instantiate(
/usr/local/lib/python3.10/site-packages/factory/base.py:317: in instantiate
    return self.factory._create(model, *args, **kwargs)
../ckan/ckan/tests/factories.py:220: in _create
    return cls.api_create(kwargs)
../ckan/ckan/tests/factories.py:203: in api_create
    result = helpers.call_action(cls._meta.action, **data_dict)
../ckan/ckan/tests/helpers.py:128: in call_action
    return logic.get_action(action_name)(context, kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

context = {'ignore_auth': True, 'user': 'test.ckan.net'}
data_dict = {'name': 'dataset-yydx-3920-kong', 'notes': 'Political officer door feeling and west own manager.', 'title': 'Fear material husband cold.'}

    def package_create(
            context: Context, data_dict: DataDict) -> ActionResult.PackageCreate:
        '''Create a new dataset (package).
    
        You must be authorized to create new datasets. If you specify any groups
        for the new dataset, you must also be authorized to edit these groups.
    
        Plugins may change the parameters of this function depending on the value
        of the ``type`` parameter, see the
        :py:class:`~ckan.plugins.interfaces.IDatasetForm` plugin interface.
    
        :param name: the name of the new dataset, must be between 2 and 100
            characters long and contain only lowercase alphanumeric characters,
            ``-`` and ``_``, e.g. ``'warandpeace'``
        :type name: string
        :param title: the title of the dataset (optional, default: same as
            ``name``)
        :type title: string
        :param private: If ``True`` creates a private dataset
        :type private: bool
        :param author: the name of the dataset's author (optional)
        :type author: string
        :param author_email: the email address of the dataset's author (optional)
        :type author_email: string
        :param maintainer: the name of the dataset's maintainer (optional)
        :type maintainer: string
        :param maintainer_email: the email address of the dataset's maintainer
            (optional)
        :type maintainer_email: string
        :param license_id: the id of the dataset's license, see
            :py:func:`~ckan.logic.action.get.license_list` for available values
            (optional)
        :type license_id: license id string
        :param notes: a description of the dataset (optional)
        :type notes: string
        :param url: a URL for the dataset's source (optional)
        :type url: string
        :param version: (optional)
        :type version: string, no longer than 100 characters
        :param state: the current state of the dataset, e.g. ``'active'`` or
            ``'deleted'``, only active datasets show up in search results and
            other lists of datasets, this parameter will be ignored if you are not
            authorized to change the state of the dataset (optional, default:
            ``'active'``)
        :type state: string
        :param type: the type of the dataset (optional),
            :py:class:`~ckan.plugins.interfaces.IDatasetForm` plugins
            associate themselves with different dataset types and provide custom
            dataset handling behaviour for these types
        :type type: string
        :param resources: the dataset's resources, see
            :py:func:`resource_create` for the format of resource dictionaries
            (optional)
        :type resources: list of resource dictionaries
        :param tags: the dataset's tags, see :py:func:`tag_create` for the format
            of tag dictionaries (optional)
        :type tags: list of tag dictionaries
        :param extras: the dataset's extras (optional), extras are arbitrary
            (key: value) metadata items that can be added to datasets, each extra
            dictionary should have keys ``'key'`` (a string), ``'value'`` (a
            string)
        :type extras: list of dataset extra dictionaries
        :param plugin_data: private package data belonging to plugins.
            Only sysadmin users may set this value. It should be a dict that can
            be dumped into JSON, and plugins should namespace their data with the
            plugin name to avoid collisions with other plugins, eg::
    
                {
                    "name": "test-dataset",
                    "plugin_data": {
                        "plugin1": {"key1": "value1"},
                        "plugin2": {"key2": "value2"}
                    }
                }
        :type plugin_data: dict
        :param relationships_as_object: see :py:func:`package_relationship_create`
            for the format of relationship dictionaries (optional)
        :type relationships_as_object: list of relationship dictionaries
        :param relationships_as_subject: see :py:func:`package_relationship_create`
            for the format of relationship dictionaries (optional)
        :type relationships_as_subject: list of relationship dictionaries
        :param groups: the groups to which the dataset belongs (optional), each
            group dictionary should have one or more of the following keys which
            identify an existing group:
            ``'id'`` (the id of the group, string), or ``'name'`` (the name of the
            group, string),  to see which groups exist
            call :py:func:`~ckan.logic.action.get.group_list`
        :type groups: list of dictionaries
        :param owner_org: the id of the dataset's owning organization, see
            :py:func:`~ckan.logic.action.get.organization_list` or
            :py:func:`~ckan.logic.action.get.organization_list_for_user` for
            available values. This parameter can be made optional if the config
            option :ref:`ckan.auth.create_unowned_dataset` is set to ``True``.
        :type owner_org: string
    
        :returns: the newly created dataset (unless 'return_id_only' is set to True
                  in the context, in which case just the dataset id will
                  be returned)
        :rtype: dictionary
    
        '''
>       model = context['model']
E       KeyError: 'model'

../ckan/ckan/logic/action/create.py:156: KeyError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:53,936 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:53,936 INFO  [ckan.model] Database initialised
2026-01-10 07:49:53,936 INFO  [ckan.model] Database rebuilt
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt
_____________ TestAutoPopulateDataDictionaries.test_simple_schema ______________

self = <ckanext.unaids.tests.test_logic.TestAutoPopulateDataDictionaries object at 0x76b78fc66080>
mocker = <pytest_mock.plugin.MockerFixture object at 0x76b78ea87a90>

    def test_simple_schema(self, mocker):
        context = {}
>       dataset = factories.Dataset()

ckanext/unaids/tests/test_logic.py:113: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/usr/local/lib/python3.10/site-packages/factory/base.py:40: in __call__
    return cls.create(**kwargs)
/usr/local/lib/python3.10/site-packages/factory/base.py:528: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
/usr/local/lib/python3.10/site-packages/factory/alchemy.py:60: in _generate
    return super()._generate(strategy, params)
/usr/local/lib/python3.10/site-packages/factory/base.py:465: in _generate
    return step.build()
/usr/local/lib/python3.10/site-packages/factory/builder.py:274: in build
    instance = self.factory_meta.instantiate(
/usr/local/lib/python3.10/site-packages/factory/base.py:317: in instantiate
    return self.factory._create(model, *args, **kwargs)
../ckan/ckan/tests/factories.py:220: in _create
    return cls.api_create(kwargs)
../ckan/ckan/tests/factories.py:203: in api_create
    result = helpers.call_action(cls._meta.action, **data_dict)
../ckan/ckan/tests/helpers.py:128: in call_action
    return logic.get_action(action_name)(context, kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

context = {'ignore_auth': True, 'user': 'test.ckan.net'}
data_dict = {'name': 'dataset-gnaz-2162-lzib', 'notes': 'Factor energy form cell coach consider person.', 'title': 'Actually arrive collection specific power.'}

    def package_create(
            context: Context, data_dict: DataDict) -> ActionResult.PackageCreate:
        '''Create a new dataset (package).
    
        You must be authorized to create new datasets. If you specify any groups
        for the new dataset, you must also be authorized to edit these groups.
    
        Plugins may change the parameters of this function depending on the value
        of the ``type`` parameter, see the
        :py:class:`~ckan.plugins.interfaces.IDatasetForm` plugin interface.
    
        :param name: the name of the new dataset, must be between 2 and 100
            characters long and contain only lowercase alphanumeric characters,
            ``-`` and ``_``, e.g. ``'warandpeace'``
        :type name: string
        :param title: the title of the dataset (optional, default: same as
            ``name``)
        :type title: string
        :param private: If ``True`` creates a private dataset
        :type private: bool
        :param author: the name of the dataset's author (optional)
        :type author: string
        :param author_email: the email address of the dataset's author (optional)
        :type author_email: string
        :param maintainer: the name of the dataset's maintainer (optional)
        :type maintainer: string
        :param maintainer_email: the email address of the dataset's maintainer
            (optional)
        :type maintainer_email: string
        :param license_id: the id of the dataset's license, see
            :py:func:`~ckan.logic.action.get.license_list` for available values
            (optional)
        :type license_id: license id string
        :param notes: a description of the dataset (optional)
        :type notes: string
        :param url: a URL for the dataset's source (optional)
        :type url: string
        :param version: (optional)
        :type version: string, no longer than 100 characters
        :param state: the current state of the dataset, e.g. ``'active'`` or
            ``'deleted'``, only active datasets show up in search results and
            other lists of datasets, this parameter will be ignored if you are not
            authorized to change the state of the dataset (optional, default:
            ``'active'``)
        :type state: string
        :param type: the type of the dataset (optional),
            :py:class:`~ckan.plugins.interfaces.IDatasetForm` plugins
            associate themselves with different dataset types and provide custom
            dataset handling behaviour for these types
        :type type: string
        :param resources: the dataset's resources, see
            :py:func:`resource_create` for the format of resource dictionaries
            (optional)
        :type resources: list of resource dictionaries
        :param tags: the dataset's tags, see :py:func:`tag_create` for the format
            of tag dictionaries (optional)
        :type tags: list of tag dictionaries
        :param extras: the dataset's extras (optional), extras are arbitrary
            (key: value) metadata items that can be added to datasets, each extra
            dictionary should have keys ``'key'`` (a string), ``'value'`` (a
            string)
        :type extras: list of dataset extra dictionaries
        :param plugin_data: private package data belonging to plugins.
            Only sysadmin users may set this value. It should be a dict that can
            be dumped into JSON, and plugins should namespace their data with the
            plugin name to avoid collisions with other plugins, eg::
    
                {
                    "name": "test-dataset",
                    "plugin_data": {
                        "plugin1": {"key1": "value1"},
                        "plugin2": {"key2": "value2"}
                    }
                }
        :type plugin_data: dict
        :param relationships_as_object: see :py:func:`package_relationship_create`
            for the format of relationship dictionaries (optional)
        :type relationships_as_object: list of relationship dictionaries
        :param relationships_as_subject: see :py:func:`package_relationship_create`
            for the format of relationship dictionaries (optional)
        :type relationships_as_subject: list of relationship dictionaries
        :param groups: the groups to which the dataset belongs (optional), each
            group dictionary should have one or more of the following keys which
            identify an existing group:
            ``'id'`` (the id of the group, string), or ``'name'`` (the name of the
            group, string),  to see which groups exist
            call :py:func:`~ckan.logic.action.get.group_list`
        :type groups: list of dictionaries
        :param owner_org: the id of the dataset's owning organization, see
            :py:func:`~ckan.logic.action.get.organization_list` or
            :py:func:`~ckan.logic.action.get.organization_list_for_user` for
            available values. This parameter can be made optional if the config
            option :ref:`ckan.auth.create_unowned_dataset` is set to ``True``.
        :type owner_org: string
    
        :returns: the newly created dataset (unless 'return_id_only' is set to True
                  in the context, in which case just the dataset id will
                  be returned)
        :rtype: dictionary
    
        '''
>       model = context['model']
E       KeyError: 'model'

../ckan/ckan/logic/action/create.py:156: KeyError
---------------------------- Captured stdout setup -----------------------------
2026-01-10 07:49:54,034 INFO  [ckan.model] Database table data deleted
2026-01-10 07:49:54,034 INFO  [ckan.model] Database initialised
2026-01-10 07:49:54,034 INFO  [ckan.model] Database rebuilt
------------------------------ Captured log setup ------------------------------
INFO     ckan.model:__init__.py:291 Database table data deleted
INFO     ckan.model:__init__.py:228 Database initialised
INFO     ckan.model:__init__.py:266 Database rebuilt

---------- coverage: platform linux, python 3.10.19-final-0 ----------
Name                                                        Stmts   Miss  Cover
-------------------------------------------------------------------------------
ckanext/unaids/__init__.py                                      6      3    50%
ckanext/unaids/actions.py                                     167     93    44%
ckanext/unaids/auth.py                                         24     16    33%
ckanext/unaids/auth_logic.py                                   88     40    55%
ckanext/unaids/blueprints/__init__.py                          10      0   100%
ckanext/unaids/blueprints/dataset_lock.py                      21     12    43%
ckanext/unaids/blueprints/login_register_catch.py               9      1    89%
ckanext/unaids/blueprints/members_list.py                      27     18    33%
ckanext/unaids/blueprints/profile_editor_data_receiver.py      11      4    64%
ckanext/unaids/blueprints/svg_map_options.py                   26     14    46%
ckanext/unaids/blueprints/unaids_dataset_releases.py           94     63    33%
ckanext/unaids/blueprints/unaids_dataset_transfer.py           25     12    52%
ckanext/unaids/blueprints/user_info_blueprint.py               20      9    55%
ckanext/unaids/blueprints/validate_user_affiliation.py         27     14    48%
ckanext/unaids/command.py                                      15      7    53%
ckanext/unaids/custom_user_profile/actions.py                  83     64    23%
ckanext/unaids/custom_user_profile/logic.py                    43     34    21%
ckanext/unaids/dataset_transfer/__init__.py                     6      3    50%
ckanext/unaids/dataset_transfer/logic.py                       38     28    26%
ckanext/unaids/dataset_transfer/model.py                       23      0   100%
ckanext/unaids/helpers.py                                     188    143    24%
ckanext/unaids/licenses.py                                      9      1    89%
ckanext/unaids/logic.py                                        46     39    15%
ckanext/unaids/plugin.py                                      203    103    49%
ckanext/unaids/tests/__init__.py                               21      9    57%
ckanext/unaids/tests/conftest.py                               39      7    82%
ckanext/unaids/tests/test_actions.py                          108     62    43%
ckanext/unaids/tests/test_actions_dataset_lock.py              52      4    92%
ckanext/unaids/tests/test_actions_show_for_release.py          44     30    32%
ckanext/unaids/tests/test_auth.py                              43     25    42%
ckanext/unaids/tests/test_auth_logic.py                       186     88    53%
ckanext/unaids/tests/test_blueprints.py                        45     23    49%
ckanext/unaids/tests/test_dataset_releases.py                 185    136    26%
ckanext/unaids/tests/test_dataset_transfer.py                  95     73    23%
ckanext/unaids/tests/test_giftless_backend.py                  35     20    43%
ckanext/unaids/tests/test_helpers.py                           14      5    64%
ckanext/unaids/tests/test_logic.py                             63     38    40%
ckanext/unaids/tests/test_plugin.py                           109     27    75%
ckanext/unaids/tests/test_scheming_schemas/__init__.py          0      0   100%
ckanext/unaids/tests/test_validators.py                        39     19    51%
ckanext/unaids/validators.py                                   38     14    63%
-------------------------------------------------------------------------------
TOTAL                                                        2325   1301    44%

=========================== short test summary info ============================
FAILED ckanext/unaids/tests/test_actions_dataset_lock.py::TestDatasetUnlock::test_metadata_updated
FAILED ckanext/unaids/tests/test_actions_dataset_lock.py::TestDatasetUnlock::test_version_deleted
FAILED ckanext/unaids/tests/test_actions_dataset_lock.py::TestDatasetUnlock::test_version_already_deleted
FAILED ckanext/unaids/tests/test_logic.py::TestAutoPopulateDataDictionaries::test_no_schema
FAILED ckanext/unaids/tests/test_logic.py::TestAutoPopulateDataDictionaries::test_missing_schema
FAILED ckanext/unaids/tests/test_logic.py::TestAutoPopulateDataDictionaries::test_simple_schema
ERROR ckanext/unaids/tests/test_actions.py::TestGetTableSchema::test_schema_returned_successfully
ERROR ckanext/unaids/tests/test_actions.py::TestGetTableSchema::test_empty_dict_returned_for_no_schema
ERROR ckanext/unaids/tests/test_actions.py::TestPackageActivityList::test_package_activity_list_containes_releases_names
ERROR ckanext/unaids/tests/test_actions.py::TestFormatGuess::test_format_guess_csv[art.csv-text/csv-CSV]
ERROR ckanext/unaids/tests/test_actions.py::TestFormatGuess::test_format_guess_csv[anc.xls-application/vnd.ms-excel-XLS]
ERROR ckanext/unaids/tests/test_actions.py::TestFormatGuess::test_format_guess_csv[country_regions.geojson-application/geo+json-GeoJSON]
ERROR ckanext/unaids/tests/test_actions.py::TestFormatGuess::test_format_guess_csv[spectrum_file.pjnz-application/pjnz-PJNZ]
ERROR ckanext/unaids/tests/test_actions.py::TestFormatGuess::test_format_guess_csv[no_file_extension-None-None]
ERROR ckanext/unaids/tests/test_actions.py::TestFormatGuess::test_format_guess_csv[-None-None]
ERROR ckanext/unaids/tests/test_actions.py::TestUserShowMe::test_no_user - ck...
ERROR ckanext/unaids/tests/test_actions.py::TestUserShowMe::test_user - ckan....
ERROR ckanext/unaids/tests/test_actions.py::TestPopulateDataDictionary::test_expected_use
ERROR ckanext/unaids/tests/test_actions.py::TestUserAffiliation::test_user_show_with_affiliation
ERROR ckanext/unaids/tests/test_actions.py::TestUserAffiliation::test_user_list_all_fields_with_affiliation
ERROR ckanext/unaids/tests/test_actions.py::TestUserAffiliation::test_user_list_usernames
ERROR ckanext/unaids/tests/test_actions.py::TestPackageCreate::test_should_complain_with_exception_when_dataset_type_invalid
ERROR ckanext/unaids/tests/test_actions.py::TestPackageCreate::test_create_dataset_without_type_creates_one_with_default_type_of_dataset
ERROR ckanext/unaids/tests/test_actions.py::TestPackageCreate::test_create_dataset_with_valid_type
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_displays_release[id]
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_displays_release[name]
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_raises_when_incorrect_release
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_returns_correct_download_links
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_only_returns_updated_resource_download_links_for_uploads
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_auth[admin-True]
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_auth[editor-True]
ERROR ckanext/unaids/tests/test_actions_show_for_release.py::TestDatasetShowForRelease::test_package_show_auth[member-True]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_unaids_organization_update_valid
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_unaids_organization_update_invalid
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_dataset_lock[True-outcome0]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_dataset_lock[False-outcome1]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[False-False-False-outcome0]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[False-True-False-outcome1]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[False-False-True-outcome2]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[False-True-True-outcome3]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[True-False-False-outcome4]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[True-True-False-outcome5]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[True-False-True-outcome6]
ERROR ckanext/unaids/tests/test_auth.py::TestAuth::test_locked_package_update[True-True-True-outcome7]
ERROR ckanext/unaids/tests/test_auth_logic.py::TestVerifyRequiredScope::test_should_pass_when_scope_is_right
ERROR ckanext/unaids/tests/test_auth_logic.py::TestVerifyRequiredScope::test_should_complain_when_scope_is_none
ERROR ckanext/unaids/tests/test_auth_logic.py::TestVerifyRequiredScope::test_should_complain_when_scope_is_incorrect
ERROR ckanext/unaids/tests/test_auth_logic.py::TestAccessTokenPresentAndValidAndUserAuthorized::test_should_set_user_when_token_correct_and_user_authorized
ERROR ckanext/unaids/tests/test_auth_logic.py::TestAccessTokenPresentAndValidAndUserAuthorized::test_should_return_false_when_no_token
ERROR ckanext/unaids/tests/test_auth_logic.py::TestAccessTokenPresentAndValidAndUserAuthorized::test_should_throw_when_subject_of_different_origin_than_auth0
ERROR ckanext/unaids/tests/test_auth_logic.py::TestValidateAndDecodeToken::test_should_validate_and_decode_token
ERROR ckanext/unaids/tests/test_auth_logic.py::TestValidateAndDecodeToken::test_should_throw_when_no_matching_rsa_key
ERROR ckanext/unaids/tests/test_auth_logic.py::TestValidateAndDecodeToken::test_should_throw_when_token_expired
ERROR ckanext/unaids/tests/test_auth_logic.py::TestValidateAndDecodeToken::test_should_throw_when_incorrect_claims
ERROR ckanext/unaids/tests/test_auth_logic.py::TestValidateAndDecodeToken::test_should_throw_when_general_exception_thrown
ERROR ckanext/unaids/tests/test_auth_logic.py::TestRegressionOAuth2PluginDoesntPreventVanillaCkanAuthentication::test_using_api_key
ERROR ckanext/unaids/tests/test_auth_logic.py::TestRegressionOAuth2PluginDoesntPreventVanillaCkanAuthentication::test_using_api_token
ERROR ckanext/unaids/tests/test_blueprints.py::TestValidateUserProfileBlueprint::test_annonymous_access_to_index_page
ERROR ckanext/unaids/tests/test_blueprints.py::TestMemberLists::test_org_member_download_200_ok
ERROR ckanext/unaids/tests/test_blueprints.py::TestMemberLists::test_org_member_download_expected_columns
ERROR ckanext/unaids/tests/test_blueprints.py::TestMemberLists::test_org_member_download_expected_emails
ERROR ckanext/unaids/tests/test_blueprints.py::TestMemberLists::test_org_member_download_403
ERROR ckanext/unaids/tests/test_blueprints.py::TestMemberLists::test_org_member_download_404
ERROR ckanext/unaids/tests/test_blueprints.py::TestMemberLists::test_org_member_download_anonymous_access
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseCreateAndEdit::test_create_with_valid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseCreateAndEdit::test_create_with_invalid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseCreateAndEdit::test_create_with_existing_activity_id
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseCreateAndEdit::test_create_with_existing_release_name
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseCreateAndEdit::test_edit_with_valid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseCreateAndEdit::test_edit_with_invalid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseDelete::test_delete_with_valid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseDelete::test_delete_with_invalid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseRestore::test_restore_with_valid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseRestore::test_restore_with_invalid_user
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseListView::test_no_releases_created
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseListView::test_releases_are_listed_to_owners
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseListView::test_releases_are_listed_to_outsiders
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseListView::test_add_release_button_is_shown_to_owners
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetReleaseListView::test_add_release_button_is_not_shown_to_outsiders
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetRead::test_activity_id_query_param
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetRead::test_when_no_release_for_dataset
ERROR ckanext/unaids/tests/test_dataset_releases.py::TestDatasetRead::test_when_on_latest_version_of_dataset_has_release
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_collaborator_transfering_dataset
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_get_org_admins_with_email_addresses_two_admins
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_get_org_admins_with_email_addresses_one_admin
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_get_org_admins_with_email_addresses_no_admins
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_get_org_admins_with_email_addresses_two_orgs
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_get_org_admins_with_no_users_in_org
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_send_dataset_transfer_emails
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_send_dataset_transfer_email_subject
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_send_dataset_transfer_emails_errors
ERROR ckanext/unaids/tests/test_dataset_transfer.py::TestDatasetTransfer::test_dataset_transfer_request
ERROR ckanext/unaids/tests/test_giftless_backend.py::TestResourceUrlEncoding::test_resource_url_encoding_test
ERROR ckanext/unaids/tests/test_helpers.py::TestDatasetLockHelper::test_dataset_lockable
ERROR ckanext/unaids/tests/test_helpers.py::TestDatasetLockHelper::test_dataset_not_lockable
ERROR ckanext/unaids/tests/test_helpers.py::TestDatasetLockHelper::test_dataset_not_found
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[validates if all upload fields correct]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[validates if no upload fields present]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[validates if all fields are present but empty]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if some fields missing and others set to empty string]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if size None]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if only size present]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if sha256 empty string]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if sha256 None]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if only sha256 present]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if invalid sha256]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if lfs_prefix empty string]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if lfs_prefix None]
ERROR ckanext/unaids/tests/test_logic.py::test_validate_resource_upload_fields[fails if only lfs_prefix present]
ERROR ckanext/unaids/tests/test_logic.py::test_update_filename_in_upload_resource_url
ERROR ckanext/unaids/tests/test_logic.py::test_update_filename_in_link_resource_url[http url]
ERROR ckanext/unaids/tests/test_logic.py::test_update_filename_in_link_resource_url[https url]
ERROR ckanext/unaids/tests/test_logic.py::test_update_filename_in_link_resource_url[url with path]
ERROR ckanext/unaids/tests/test_plugin.py::TestPlugin::test_geojson_format_guessed_correctly
ERROR ckanext/unaids/tests/test_plugin.py::TestPlugin::test_blob_storage_validator_is_used_during_resource_actions
ERROR ckanext/unaids/tests/test_plugin.py::TestValidatePackage::test_validate_package
ERROR ckanext/unaids/tests/test_plugin.py::TestValidatePackage::test_metadata_change_does_not_validate_package
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_organisation_id_exists_validator
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_if_empty_guess_format[None-None-http://adr.local/resource/8919/demo.csv-text/csv]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_if_empty_guess_format[None-None-http://adr.local/resource/8919/demo.geojson-application/geo+json]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_if_empty_guess_format[None-None-http://adr.local/resource/8919/demo.pjnz-application/pjnz]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_if_empty_guess_format[text/csv-8919-http://adr.local/resource/89192/demo.xlsx-text/csv]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_if_empty_guess_format[None-8919-http://adr.local/resource/8919/demo.csv-None]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context0-result0]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context1-result1]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context2-result2]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context3-result3]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context4-result4]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context5-result5]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context6-result6]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context7-result7]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_raises[context8-result8]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_sets_default_value[context0-data0-false]
ERROR ckanext/unaids/tests/test_validators.py::TestValidators::test_read_only_sets_default_value[context1-data1-false]
===== 6 failed, 27 passed, 1 skipped, 1337 warnings, 128 errors in 51.12s ======
